LLM Suite Response
When creating an AWS IAM policy in Terraform to allow access to an S3 bucket, you have a couple of options for handling the bucket name. Both approaches have their pros and cons, and the best choice depends on your specific use case and preferences.

Hardcoding the Bucket Name:

Pros:
Simplicity: It's straightforward and easy to implement.
No need to import existing resources: If the bucket already exists and you don't need to manage it with Terraform, hardcoding can be a quick solution.
Cons:
Less flexibility: If the bucket name changes, you'll need to update the policy manually.
Harder to manage at scale: If you have multiple environments or buckets, hardcoding can become cumbersome.
Importing the S3 Bucket and Referencing It:

Pros:
Flexibility: You can easily reference the bucket name using Terraform variables or outputs, making it easier to manage changes.
Consistency: If you're managing the bucket with Terraform, it ensures that your infrastructure is defined in a single place.
Easier to manage at scale: Using variables or outputs can simplify managing multiple environments or buckets.
Cons:
Complexity: Requires importing the bucket into Terraform if it's not already managed, which adds an extra step.
Example of Hardcoding the Bucket Name:
hcl



resource "aws_iam_policy" "s3_access_policy" {
  name        = "S3AccessPolicy"
  description = "Policy to allow access to a specific S3 bucket"

  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Effect   = "Allow"
        Action   = ["s3:GetObject", "s3:PutObject"]
        Resource = "arn:aws:s3:::your-bucket-name/*"
      }
    ]
  })
}
Example of Referencing the Bucket Name:
First, import the existing bucket if it's not managed by Terraform:

bash



terraform import aws_s3_bucket.example your-bucket-name
Then, reference it in your policy:

hcl



data "aws_s3_bucket" "example" {
  bucket = "your-bucket-name"
}

resource "aws_iam_policy" "s3_access_policy" {
  name        = "S3AccessPolicy"
  description = "Policy to allow access to a specific S3 bucket"

  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Effect   = "Allow"
        Action   = ["s3:GetObject", "s3:PutObject"]
        Resource = "arn:aws:s3:::${data.aws_s3_bucket.example.bucket}/*"
      }
    ]
  })
}
In summary, if you anticipate changes to the bucket name or need to manage multiple environments, importing and referencing the bucket is a more flexible and scalable approach. If it's a one-off or simple setup, hardcoding might be sufficient.






so what is difference between rate) and deriv) functions?
The rate() and deriv() functions in Prometheus are both used to analyze the rate of change of
time series data, but they are used in different contexts and have different purposes:
1. rate() Function:
• Purpose: The rate() function is specifically designed for calculating the per-second average rate of increase of a counter over a specified time range. It is typically used with metrics that are counters, which are monotonically increasing values that reset to zero upon restart.
• Use Case: It's commonly used for metrics like request counts, error counts, or any other metric that accumulates over time.
• Behavior: rate() calculates the average rate of increase by looking at the difference between the start and end values of the counter within the specified time window and dividing by the duration of the window.
2. deriv() Function:
• Purpose: The deriv() function calculates the per-second derivative of a time series, which can be used for both counters and gauges. It estimates the instantaneous rate of change at each point in time.
• Use Case: It's useful for metrics that can both increase and decrease, such as gauges, or when you want to understand the trend or slope of a metric over time.
• Behavior: deriv() uses linear regression to estimate the slope of the time series data over the specified time window, providing a more precise measure of the rate of change at each point.
In summary, use rate() when dealing with counters to get the average rate of increase, and use deriv() when you need to calculate the instantaneous rate of change for any type of time series, including gauges.



sum(deriv(kafka_consumergroup_lag/consumergroup="borrow-fa-gateway",
topic="chase.borrow.fagateway.balance-internal-event-v001.triage"}[5m])) by (topic). explain what does
"deriv" function doing here?

In the context of Prometheus, the deriv function is used to calculate the per-second derivative of a time series. It estimates the rate of change of the metric over time, which is particularly useful for understanding trends or changes in metrics that are expected to vary over time.
Here's a breakdown of what the deriv function is doing in your query:
• kafka_consumergroup_
_lag{consumergroup="borrow-fa-gateway"
topic="chase.borrow.fagateway.balance-internal-event-v001. triage"} [5ml : This part of the query selects the time series data for the specified Kafka consumer group lag over the last 5 minutes.
• deriv(...): The deriv function is applied to this time series data to compute the rate of change (derivative) per second. It essentially tells you how quickly the consumer group lag is increasing or decreasing over the specified time window.
• sum(... by (topic)) : After calculating the derivative, the sum function aggregates the results by the topic label. This means it sums up the rate of change for all instances of the specified topic, providing a single value that represents the overall trend for that topic.
In summary, the deriv function in this query is used to determine how the lag for the specified Kafka consumer group and topic is changing over time, and the result is aggregated by topic to provide an overall view of the trend.

Job creation. A CronJob creates a Job object approximately once per execution time of its schedule. The scheduling is approximate because there are certain circumstances where two Jobs might be created, or no Job might be created. Kubernetes tries to avoid those situations, but does not completely prevent them.

Concurrency policy

The .spec.concurrencyPolicy field is also optional. It specifies how to treat concurrent executions of a Job that is created by this CronJob. The spec may specify only one of the following concurrency policies:

• Allow (default): The Cronjob allows concurrently running Jobs

• Forbid: The Cronjob does not allow concurrent runs; if it is time for a new Job run and the previous Job run hasn't finished yet, the Cronjob skips the new Job run. Also note that when the previous Job run finishes, .spec.startingDeadlineSeconds is still taken into account and may result in a new Job run.

• Replace : If it is time for a new Job run and the previous Job run hasn't finished yet, the CronJob replaces the currently running Job run with a new Job run

Note that concurrency policy only applies to the Jobs created by the same CronJob. If there are multiple Cronjobs, their respective Jobs are always allowed to run concurrently.




will a kubernetes cronjob create its pod right before the scheduled time or when it is created?
A Kubernetes Cronjob creates its pod at the scheduled time, not when the Cronjob itself is created.
Cronjobs in Kubernetes are used to run tasks at specific times or intervals, similar to how cron jobs work in Unix-like operating systems. When you define a Cronjob, you specify a schedule using cron syntax, and Kubernetes will create a job and its associated pod at the specified time according to that schedule.
The pod is not created until the scheduled time arrives.


what is the difference between "http_server_requests_seconds_bucket" and
"http_server_requests_seconds_count" in promql?
In Prometheus, when dealing with metrics related to HTTP server requests, you might encounter metrics like http_server_requests_seconds_bucket and http_server_requests_ seconds_count.
These are typically part of a histogram metric type, which is used to observe the distribution of request durations.
Here's the difference between the two:
1. http_server_requests_seconds_bucket :
• This metric represents the count of requests that have a duration less than or equal to a specific bucket boundary.
• Histograms in Prometheus are defined with multiple buckets, each representing a range of values. For example, you might have buckets for request durations like 0.1s, 0.5s, 1s, etc.
• The http_server_requests_seconds_bucket metric will have a label called le (less than or equal), which indicates the upper bound of the bucket. For example, le="0.5" would count all requests that took 0.5 seconds or less.
• This metric is useful for calculating quantiles, such as the 95th percentile of request durations.
2. http_server_requests_seconds_count :
• This metric represents the total count of all observed requests, regardless of their duration.
• It is essentially the sum of all the counts in the
http_server_requests_seconds_bucket metrics for all bucket boundaries.
• This metric is useful for calculating the total number of requests over a period of time.
In summary, http_server_requests_seconds_bucket is used to understand the distribution of request durations across different buckets, while http_server_requests_seconds_count provides the total number of requests observed. Together, they help in analyzing the performance and behavior of HTTP server requests.


In Bash, the -ecx string is likely a combination of options that are being passed to the shell. Here's a breakdown of what each option typically means:
• -e : This option tells the shell to exit immediately if any command within a script or a command sequence fails (i.e., returns a non-zero status). This is useful for error handling, as it prevents the script from continuing execution after an error occurs.
• -c: This option is used to pass a command string to the shell. When you use bash -c
"command"
, the shell executes the command string provided. This is often used to execute a
command or a series of commands from within a script or another program.
• —X : This option enables a mode of the shell where all executed commands are printed to the terminal (standard error) before they are executed. This is useful for debugging, as it allows you to see exactly what commands are being run.
When combined as -ecx, these options would typically be used to run a command with error checking and debugging output enabled. However, the order of options doesn't matter, so -ecx is equivalent to
—X —e -c.

create a cron schedule that runs every night at 3:30 am

LLM Suite Response
To create a cron schedule that runs every night at 3:30 AM, you would use the following cron expression:

30 3 * * *
Here's a breakdown of the cron expression:

30 - This specifies the minute of the hour when the task should run (30 minutes past the hour).
3 - This specifies the hour of the day when the task should run (3 AM).
* - This wildcard means "every" for the day of the month.
* - This wildcard means "every" for the month.
* - This wildcard means "every" for the day of the week.
So, this cron job will execute at 3:30 AM every day.


LLM Suite Response
To determine if a Kubernetes CronJob ran successfully, you can use metrics related to the underlying Jobs that the CronJob creates. Specifically, you can look at the kube_job_status_succeeded metric, which provides information about the number of successful completions of a Job.

Here's how you can use this metric:

kube_job_status_succeeded: This metric indicates the number of times a Job has completed successfully. It typically includes labels such as namespace and job_name, which you can use to filter for the specific Job associated with your CronJob.
To monitor the success of a CronJob, you would:

Identify the Jobs created by the CronJob. Each execution of a CronJob results in a new Job, and these Jobs will have names derived from the CronJob's name.

Use the kube_job_status_succeeded metric to check the success count for these Jobs. A non-zero value indicates that the Job has completed successfully.

Additionally, you might also want to look at:

kube_job_status_failed: This metric indicates the number of times a Job has failed. Monitoring this can help you identify any failures in the execution of your CronJobs.
By combining these metrics, you can get a comprehensive view of whether your CronJobs are running successfully and identify any issues that may arise.


User Prompt
this is for "job" but i need something similar for a "cronjob"

LLM Suite Response
For monitoring the success of Kubernetes CronJobs specifically, you typically rely on the metrics of the Jobs they create, as CronJobs themselves do not directly expose a "success" metric. However, you can infer the success of a CronJob by monitoring the Jobs it spawns.

While there isn't a direct Prometheus metric for CronJob success, you can use a combination of metrics and labels to achieve this:

kube_job_status_succeeded: As mentioned earlier, this metric provides the number of successful completions for a Job. By filtering this metric with the appropriate labels, you can track the success of Jobs created by a specific CronJob.

kube_job_labels: This metric can help you identify which Jobs are associated with a particular CronJob. It includes labels such as job_name and namespace, and you can use it to filter Jobs by the cronjob label.

To effectively monitor CronJob success, you can set up a Prometheus query that filters kube_job_status_succeeded by the cronjob label. This way, you can track the success of all Jobs created by a specific CronJob.

Here's an example query to get you started:

promql



sum by (cronjob) (kube_job_status_succeeded{namespace="your-namespace", cronjob="your-cronjob-name"})
This query will give you the total number of successful completions for all Jobs created by the specified CronJob in the given namespace. By monitoring this over time, you can determine if the CronJob is running successfully.


does kubernetes cronjob create a kubernetes job under the hood?

LLM Suite Response
Yes, a Kubernetes CronJob creates a Kubernetes Job under the hood. A CronJob in Kubernetes is used to run tasks on a schedule, similar to how cron jobs work in Unix-like operating systems. When the scheduled time for a CronJob is reached, Kubernetes automatically creates a Job resource. This Job then manages the execution of the specified task, ensuring that it runs to completion. Each execution of the CronJob results in a new Job being created, which in turn creates one or more Pods to run the task.


User Prompt
if a cronjob is called "vault-tools-republish-cron" what would be the name of its job approximately?

LLM Suite Response
When a CronJob in Kubernetes is executed, it creates a Job with a name that is derived from the CronJob's name, followed by a hyphen and a suffix that includes a timestamp or a unique identifier. For a CronJob named vault-tools-republish-cron, the Job name would typically look something like vault-tools-republish-cron-<suffix>, where <suffix> is a unique string that usually includes a timestamp indicating when the Job was created. This suffix helps ensure that each Job has a unique name, even if multiple Jobs are created from the same CronJob.




LLM Suite Response
If you want to introduce a delay after executing a script in a piped command, you can use the sleep command to pause the execution for a specified amount of time. However, when dealing with piped commands, you need to ensure that the delay is applied correctly in the sequence of commands.

Here's how you can achieve a 3-minute delay after executing a script in a piped command:

Using a Subshell: You can use a subshell to execute the script and then introduce a delay before continuing with the next command in the pipeline.

bash



(kubectl exec vault-tools-republish-cron-28941778-65k7z -n 105250-vault-operators -- ./scripts/vault_list_post_postings_failures.sh; sleep 180) | grep '"id":'
In this example, the script is executed, and then sleep 180 introduces a 3-minute delay (180 seconds) before the output is piped to grep.

Using a Temporary File: Another approach is to redirect the output to a temporary file, introduce a delay, and then process the file.

bash



kubectl exec vault-tools-republish-cron-28941778-65k7z -n 105250-vault-operators -- ./scripts/vault_list_post_postings_failures.sh > temp_output.txt
sleep 180
grep '"id":' temp_output.txt
rm temp_output.txt
This method captures the output in a file, waits for 3 minutes, processes the file with grep, and then removes the temporary file.

Both methods allow you to introduce a delay after executing the script while still being able to process the output with subsequent commands.


User Prompt
create a loop in shell that writes a number in order of 1,2,3,4,.... every second until number 100

LLM Suite Response
You can create a loop in a shell script that writes numbers in order from 1 to 100, with a one-second delay between each number, using the seq command and a for loop. Here's how you can do it:

bash



#!/bin/bash

for i in $(seq 1 100); do
  echo $i
  sleep 1
done
Explanation:
#!/bin/bash: This is the shebang line that tells the system to use the Bash shell to execute the script.
for i in $(seq 1 100): This loop iterates over the sequence of numbers from 1 to 100. The seq command generates this sequence.
echo $i: This command prints the current number ($i) to the terminal.
sleep 1: This command pauses the execution for 1 second before continuing to the next iteration of the loop.
To run this script, you can save it to a file, for example, count.sh, and then execute it with the following commands:

bash



chmod +x count.sh
./count.sh
This will print numbers from 1 to 100, one per second, to the terminal.


User Prompt
kubectl exec vault-tools-republish-cron-28941778-65k7z -n 105250-vault-operators -- ./scripts/vault_list_post_postings_failures.sh > /tmp/temp_ids.txt   this command will make the file inside the local machine, but i want it to be created inside the same pod

LLM Suite Response
To create a file inside the same pod where the script is executed, you need to redirect the output within the context of the pod itself. You can achieve this by including the redirection as part of the command executed by kubectl exec. Here's how you can do it:

bash



kubectl exec vault-tools-republish-cron-28941778-65k7z -n 105250-vault-operators -- sh -c './scripts/vault_list_post_postings_failures.sh > /tmp/temp_ids.txt'
Explanation:
sh -c: This tells the shell to execute the following string as a command. It's necessary because the redirection (>) needs to be interpreted by the shell within the pod.
'./scripts/vault_list_post_postings_failures.sh > /tmp/temp_ids.txt': This is the command string that gets executed inside the pod. The script's output is redirected to /tmp/temp_ids.txt within the pod's filesystem.
By using this approach, the file /tmp/temp_ids.txt will be created inside the pod, not on your local machine.



LLM Suite Response
To add a sleep command to wait 300 seconds between each curl request without changing anything else in your script, you can simply insert the sleep command right after the curl command within the xargs loop. Here's how you can do it:

bash



cat $EXPORTED_RESULT_FAILURES | jq -sr ".[] ${FAILURE_CONDITION} | .account_id" | uniq \
  | xargs -I '{}' -n1 bash -c 'curl -k "${REPOSTING_URL}:republish" -X POST -H "X-Auth-Token: ${REPOSTING_TOKEN}" -H "Content-Type: Application/Json" \
    --data-binary "{\"account_id\": \"{}\",\"republish_type\": \"REPUBLISH_TYPE_REPUBLISH_BLOCKED_FAILURES\"}"; sleep 300'
In this modification, I've wrapped the curl command and the sleep command in a bash -c block. This allows both commands to be executed as part of the same xargs iteration. The sleep 300 command will pause execution for 300 seconds after each curl request.


User Prompt
instead of piping each line here to curl, use a for loop: cat $EXPORTED_RESULT_FAILURES | jq -sr ".[] ${FAILURE_CONDITION} | .account_id" | uniq \
  | xargs -I '{}' -n1 bash -c 'curl -k "${REPOSTING_URL}:republish" -X POST -H "X-Auth-Token: ${REPOSTING_TOKEN}" -H "Content-Type: Application/Json" \
    --data-binary "{\"account_id\": \"{}\",\"republish_type\": \"REPUBLISH_TYPE_REPUBLISH_BLOCKED_FAILURES\"}"; sleep 300'

LLM Suite Response
To replace the xargs command with a for loop, you can read the output of the jq command into a variable or directly iterate over it. Here's how you can achieve this:

bash



# Extract account IDs into an array
account_ids=$(cat "$EXPORTED_RESULT_FAILURES" | jq -sr ".[] ${FAILURE_CONDITION} | .account_id" | uniq)

# Iterate over each account ID
for account_id in $account_ids; do
  # Execute the curl command for each account ID
  curl -k "${REPOSTING_URL}:republish" -X POST -H "X-Auth-Token: ${REPOSTING_TOKEN}" -H "Content-Type: Application/Json" \
    --data-binary "{\"account_id\": \"${account_id}\",\"republish_type\": \"REPUBLISH_TYPE_REPUBLISH_BLOCKED_FAILURES\"}"
  
  # Sleep for 300 seconds between requests
  sleep 300
done
Explanation:
Extracting Account IDs: The jq command processes the JSON and extracts unique account IDs, which are stored in the account_ids variable.

Iterating with a For Loop: The for loop iterates over each account ID in the account_ids variable.

Executing the Curl Command: For each account ID, the curl command is executed with the appropriate data.

Sleeping Between Requests: The sleep 300 command pauses execution for 300 seconds between each curl request.

This approach replaces the xargs command with a for loop, allowing you to control the flow of execution more explicitly.



what does " jq -sr ".[] ${FAILURE_CONDITION} " is doing here:  account_ids=$(cat "$EXPORTED_RESULT_FAILURES" | jq -sr ".[] ${FAILURE_CONDITION} | .account_id" | uniq)

LLM Suite Response
The command you provided is a shell command that uses jq, a command-line JSON processor, to extract and process data from a JSON file. Let's break down the command step by step:

cat "$EXPORTED_RESULT_FAILURES": This part reads the contents of the file specified by the variable EXPORTED_RESULT_FAILURES. The file is expected to contain JSON data.

jq -sr ".[] ${FAILURE_CONDITION} | .account_id": This part processes the JSON data using jq. Here's what each part does:

-s: This option tells jq to read the entire input as a single JSON array. This is useful when the input consists of multiple JSON objects, one per line.
-r: This option tells jq to output raw strings instead of JSON-encoded strings. This is useful when you want to use the output in shell scripts.
.[]: This part iterates over each element in the JSON array.
${FAILURE_CONDITION}: This is a variable that presumably contains a jq filter condition. It is used to filter the elements of the JSON array based on some criteria.
| .account_id: This part extracts the account_id field from each element that matches the FAILURE_CONDITION.
uniq: This command removes duplicate lines from the output. It assumes that the account_id values are sorted, or it will only remove consecutive duplicates.

account_ids=$(...): This part assigns the final output, which is a list of unique account_id values, to the shell variable account_ids.

In summary, this command reads a JSON file, filters the data based on a condition specified by FAILURE_CONDITION, extracts the account_id field from each matching element, removes duplicate account_id values, and stores the result in the account_ids variable.



LLM Suite Response
To automate the creation of a service account in a specific Kubernetes namespace using a shell script, you can use the kubectl command-line tool. Below is a simple shell script that accomplishes this task. Make sure you have kubectl installed and configured to interact with your Kubernetes cluster.

bash



#!/bin/bash

# Check if the correct number of arguments is provided
if [ "$#" -ne 2 ]; then
    echo "Usage: $0 <namespace> <service-account-name>"
    exit 1
fi

# Assign command-line arguments to variables
NAMESPACE=$1
SERVICE_ACCOUNT_NAME=$2

# Create the namespace if it doesn't exist
kubectl get namespace $NAMESPACE >/dev/null 2>&1
if [ $? -ne 0 ]; then
    echo "Namespace '$NAMESPACE' does not exist. Creating it..."
    kubectl create namespace $NAMESPACE
fi

# Create the service account in the specified namespace
kubectl create serviceaccount $SERVICE_ACCOUNT_NAME --namespace $NAMESPACE

# Verify the creation of the service account
if [ $? -eq 0 ]; then
    echo "Service account '$SERVICE_ACCOUNT_NAME' created successfully in namespace '$NAMESPACE'."
else
    echo "Failed to create service account '$SERVICE_ACCOUNT_NAME' in namespace '$NAMESPACE'."
    exit 1
fi
How to Use the Script
Save the script to a file, for example, create_service_account.sh.
Make the script executable by running: chmod +x create_service_account.sh.
Run the script with the desired namespace and service account name as arguments. For example:
bash



./create_service_account.sh my-namespace my-service-account
Notes
The script first checks if the specified namespace exists. If it doesn't, the script creates the namespace.
It then creates the service account in the specified namespace.
The script provides feedback on whether the service account was created successfully or if there was an error.
Ensure that you have the necessary permissions to create namespaces and service accounts in your Kubernetes cluster.


User Prompt
what does this mean? if [ $? -ne 0 ]; then

LLM Suite Response
In a shell script, the construct if [ $? -ne 0 ]; then is used to check the exit status of the last executed command.

Here's a breakdown of what each part means:

$?: This is a special variable in shell scripting that holds the exit status of the last executed command. The exit status is a numeric value returned by a command to indicate whether it succeeded or failed. By convention, an exit status of 0 means success, while any non-zero value indicates an error or failure.

-ne: This is a comparison operator in shell scripting that stands for "not equal." It is used to compare two numbers.

0: This is the number being compared against, which represents a successful exit status.

then: This keyword indicates the start of a block of code that will be executed if the condition is true.

Putting it all together, if [ $? -ne 0 ]; then checks if the exit status of the last command is not equal to 0. If the condition is true (meaning the last command failed), the script will execute the block of code that follows the then keyword. This is commonly used for error handling in shell scripts to take specific actions when a command fails.


User Prompt
is this correct:  kubectl can-i list deployments --as system:serviceaccount:$NAMESPACE:$SERVICE_ACCOUNT_NAME
if [ $? -eq 'yes' ]; then

LLM Suite Response
The command you are using to check permissions with kubectl is almost correct, but there are a couple of issues with the if statement that follows it.

kubectl can-i Command:

The kubectl auth can-i command is used to check if a user or service account has permission to perform a specific action. The correct usage is:
bash



kubectl auth can-i list deployments --as system:serviceaccount:$NAMESPACE:$SERVICE_ACCOUNT_NAME
This command will output yes or no to indicate whether the action is allowed.
Checking the Output:

The if statement should compare the output of the kubectl auth can-i command to the string yes, not the exit status ($?). The exit status of kubectl auth can-i is always 0 unless there's an error in executing the command itself.
You can capture the output of the command and then compare it, like this:
bash



#!/bin/bash

# Check if the service account can list deployments
CAN_I=$(kubectl auth can-i list deployments --as system:serviceaccount:$NAMESPACE:$SERVICE_ACCOUNT_NAME)

if [ "$CAN_I" == "yes" ]; then
    echo "The service account can list deployments."
else
    echo "The service account cannot list deployments."
fi
Explanation:
CAN_I=$(kubectl auth can-i ...): This captures the output of the kubectl auth can-i command into the CAN_I variable.
if [ "$CAN_I" == "yes" ]; then: This checks if the output stored in CAN_I is equal to yes.
The script then prints a message based on whether the service account has the required permission.


add several labels to a kubernetes serviceaccount via commandline

LLM Suite Response
To add several labels to a Kubernetes ServiceAccount via the command line, you can use the kubectl label command. Here is how you can do it:

First, ensure you have the name of the ServiceAccount and the namespace it resides in.

Use the kubectl label command to add labels. You can add multiple labels in a single command.

Here's an example command:

bash



kubectl label serviceaccount <serviceaccount-name> -n <namespace> key1=value1 key2=value2 key3=value3
Replace <serviceaccount-name> with the name of your ServiceAccount, <namespace> with the appropriate namespace, and key1=value1, key2=value2, etc., with the labels you want to add.

For example, if you have a ServiceAccount named my-serviceaccount in the default namespace and you want to add labels env=production and team=devops, you would run:

bash



kubectl label serviceaccount my-serviceaccount -n default env=production team=devops
This command will add the specified labels to the ServiceAccount. If a label with the same key already exists, it will be updated with the new value.




LLM Suite Response
This script is designed to handle two main tasks: deleting failed entries and republishing them for specific account IDs. Let's break down what each part of the script does:

Extracting Failed IDs:

bash



failed_ids=$(cat "$EXPORTED_RESULT_FAILURES" | jq -sr ".[] ${REASON} | .id" | uniq)
This line reads a JSON file specified by the environment variable EXPORTED_RESULT_FAILURES.
It uses jq to parse the JSON and extract unique IDs of failed entries based on a specific REASON.
The -s option in jq slurps the entire input into a single array, and -r outputs raw strings.
Extracting Account IDs:

bash



account_ids=$(cat "$EXPORTED_RESULT_FAILURES" | jq -sr ".[] ${STATUS} | .account_id" | uniq)
Similar to the previous step, this line extracts unique account IDs based on a specific STATUS.
Deleting Failed Entries:

bash



for id in $failed_ids; do
  echo ${id}
  curl -k "${REPOSTING_URL}/${id}" -X DELETE -H "X-Auth-Token: ${REPOSTING_TOKEN}"
  sleep 5
done
This loop iterates over each failed ID.
It sends a DELETE request to a URL constructed with the REPOSTING_URL and the failed ID.
The request includes an authentication token in the header.
A 5-second pause (sleep 5) is added between requests to avoid overwhelming the server.
Republishing for Account IDs:

bash



echo "republishing"
for account_id in $account_ids; do
  curl -k "${REPOSTING_URL}:republish" -X POST -H "X-Auth-Token: ${REPOSTING_TOKEN}" -H "Content-Type: Application/Json" \
    --data-binary "{\"account_id\": \"${account_id}\",\"republish_type\": \"REPUBLISH_TYPE_REPUBLISH_BLOCKED_FAILURES\"}"
  sleep 5
done
This loop iterates over each account ID.
It sends a POST request to a republish endpoint with the account ID and a specific republish type in the JSON payload.
The request includes an authentication token and specifies the content type as JSON.
Again, a 5-second pause is added between requests.
Considerations:

Ensure that the environment variables (EXPORTED_RESULT_FAILURES, REPOSTING_URL, REPOSTING_TOKEN, REASON, STATUS) are correctly set before running the script.
The script assumes that the JSON structure in the file matches the queries used with jq.
The -k option in curl is used to allow connections to SSL sites without certificates, which might be necessary if the server uses self-signed certificates.
The sleep command is used to prevent sending too many requests in a short period, which could be seen as abusive by the server. Adjust the sleep duration as needed based on server capacity and rate limits.


LLM Suite Response
To modify the second loop so that it only processes IDs that are in account_ids but not in failed_ids, you can use a combination of grep and comm to filter out the IDs that are present in both lists. Here's how you can do it:

bash



# Convert the lists to arrays
failed_ids_array=($failed_ids)
account_ids_array=($account_ids)

# Create temporary files to store the IDs
failed_ids_file=$(mktemp)
account_ids_file=$(mktemp)

# Write the IDs to the temporary files
printf "%s\n" "${failed_ids_array[@]}" > "$failed_ids_file"
printf "%s\n" "${account_ids_array[@]}" > "$account_ids_file"

# Find IDs that are in account_ids but not in failed_ids
filtered_ids=$(comm -23 <(sort "$account_ids_file") <(sort "$failed_ids_file"))

# Clean up temporary files
rm "$failed_ids_file" "$account_ids_file"

echo "republishing"
for account_id in $filtered_ids; do
  curl -k "${REPOSTING_URL}:republish" -X POST -H "X-Auth-Token: ${REPOSTING_TOKEN}" -H "Content-Type: Application/Json" \
    --data-binary "{\"account_id\": \"${account_id}\",\"republish_type\": \"REPUBLISH_TYPE_REPUBLISH_BLOCKED_FAILURES\"}"
  sleep 5
done
Explanation:
Temporary Files: We use temporary files to store the IDs from failed_ids and account_ids.
comm Command: This command is used to compare two sorted files line by line. comm -23 outputs lines that are only in the first file (account_ids_file) and not in the second file (failed_ids_file).
Sorting: Both files are sorted before using comm to ensure proper comparison.
Cleanup: Temporary files are removed after use to avoid clutter.
This approach ensures that the second loop only processes IDs that are unique to account_ids and not present in failed_ids.


User Prompt
do this but without creating new files

LLM Suite Response
Certainly! You can achieve this without creating temporary files by using process substitution and arrays. Here's how you can modify the script:

bash



# Convert the lists to arrays
failed_ids_array=($failed_ids)
account_ids_array=($account_ids)

# Use process substitution and comm to filter IDs
filtered_ids=$(comm -23 <(printf "%s\n" "${account_ids_array[@]}" | sort) <(printf "%s\n" "${failed_ids_array[@]}" | sort))

echo "republishing"
for account_id in $filtered_ids; do
  curl -k "${REPOSTING_URL}:republish" -X POST -H "X-Auth-Token: ${REPOSTING_TOKEN}" -H "Content-Type: Application/Json" \
    --data-binary "{\"account_id\": \"${account_id}\",\"republish_type\": \"REPUBLISH_TYPE_REPUBLISH_BLOCKED_FAILURES\"}"
  sleep 5
done
Explanation:
Arrays: The failed_ids and account_ids are converted into arrays.
Process Substitution: comm is used with process substitution to compare the sorted output of the arrays directly.
printf and sort: printf is used to print each element of the arrays on a new line, and sort is used to ensure the input to comm is sorted.
comm -23: This command outputs lines that are only in the first input (i.e., account_ids_array) and not in the second input (i.e., failed_ids_array).
This approach avoids the need for temporary files by using in-memory operations.


LLM Suite Response
Sure! Let's imagine you have two different toys that help you understand how things change over time.

rate() is like a counting toy:

Imagine you have a toy that counts how many times you jump up and down. Every time you jump, the number goes up. If you want to know how fast you're jumping, you can look at how many jumps you did in a certain amount of time, like in one minute. That's what rate() does—it tells you how fast the number is going up.
deriv() is like a magic slope toy:

Now, imagine you have a toy car on a track that can go up and down hills. Sometimes it goes up, and sometimes it goes down. If you want to know how steep the hill is at any moment, you use the magic slope toy. It tells you if the car is going up or down and how steep the hill is. That's what deriv() does—it tells you how quickly something is changing, whether it's going up or down.
So, rate() is for counting how fast something is going up, and deriv() is for figuring out how steep the change is, whether it's going up or down.


User Prompt
what is the difference between these two promql queries: sum(deriv(kafka_consumergroup_lag{consumergroup="borrow-fa-gateway", topic="chase.borrow.fagateway.balance-internal-event-v001.triage"}[5m]))  sum(kafka_consumergroup_lag{consumergroup="borrow-fa-gateway", topic="chase.borrow.fagateway.balance-internal-event-v001.triage"}) by (topic)

LLM Suite Response
The two PromQL queries you provided are used to analyze the Kafka consumer group lag, but they do so in different ways. Let's break down the differences:

First Query: sum(deriv(...)):

Purpose: This query calculates the rate of change of the Kafka consumer group lag over a 5-minute window and then sums up these rates.
Functionality:
deriv(...): Computes the per-second rate of change (derivative) of the lag over the last 5 minutes. It tells you how quickly the lag is increasing or decreasing.
sum(...): Aggregates the rate of change values across all instances of the specified topic, providing a single value that represents the overall trend.
Use Case: This query is useful if you want to understand how the lag is changing over time, i.e., whether it's growing or shrinking and at what rate.
Second Query: sum(...) by (topic):

Purpose: This query calculates the total current lag for the specified Kafka consumer group and topic, aggregated by topic.
Functionality:
sum(...) by (topic): Sums up the current lag values for all instances of the specified topic, providing the total lag for that topic.
Use Case: This query is useful if you want to know the current total lag for the consumer group and topic, without considering how it is changing over time.
Key Differences:

The first query (sum(deriv(...))) focuses on the rate of change of the lag, providing insight into how the lag is trending over time.
The second query (sum(...) by (topic)) focuses on the current total lag, providing a snapshot of the lag at the current moment.
In summary, the first query is about understanding trends and changes in the lag, while the second query is about understanding the current state of the lag.


in promql change this status=~"50.+" so that i can also accept values like 511 or 519

LLM Suite Response
In PromQL, the regular expression status=~"50.+" is used to match status codes that start with "50" followed by one or more characters. To modify this expression to also accept values like "511" or "519", you can adjust the regular expression to match any status code starting with "5" followed by two digits. Here's how you can do it:

promql



status=~"5[0-9][0-9]"
This regular expression will match any three-digit status code that starts with "5", including "500", "511", "519", and so on.


User Prompt
no i want it to be regex

LLM Suite Response
Certainly! To create a regular expression in PromQL that matches status codes starting with "50" and also includes codes like "511" or "519", you can use the following pattern:

promql



status=~"5[0-9][0-9]"
This regular expression will match any three-digit status code that starts with "5", followed by any two digits from "00" to "99". This includes "500", "511", "519", and any other status code in that range.


User Prompt
is this also work: status=~"5.+"

LLM Suite Response
Yes, the regular expression status=~"5.+" will also work for matching status codes that start with "5" and are followed by one or more characters. This pattern will match any status code that begins with "5", including "500", "511", "519", and so on.

Here's a breakdown of the pattern:

5 matches the digit "5".
. matches any single character (digit, letter, etc.).
+ means "one or more" of the preceding element, which in this case is any character.
So, status=~"5.+" will match any status code that starts with "5" and has at least one more character following it.


LLM Suite Response
To access a Kubernetes pod via a browser, you typically need to expose the pod using a Kubernetes Service. This service can be of type NodePort, LoadBalancer, or Ingress, depending on your cluster setup and requirements. Here's a general approach using a NodePort service:

Expose the Pod with a Service:

First, you need to create a service that exposes your pod. You can do this using the kubectl expose command. For example:

bash



kubectl expose pod <pod-name> --type=NodePort --port=<port>
Replace <pod-name> with the name of your pod and <port> with the port number your application is listening on inside the pod.

Get the NodePort:

After exposing the pod, you can get the details of the service to find the NodePort assigned:

bash



kubectl get service <pod-name>
Look for the NodePort value in the output. It will be a port number in the range 30000-32767.

Access the Pod via Browser:

You can now access the application running in your pod by navigating to http://<node-ip>:<node-port> in your browser. Replace <node-ip> with the IP address of any of your cluster nodes and <node-port> with the NodePort you obtained in the previous step.

If you're running Kubernetes on a cloud provider, you might prefer using a LoadBalancer service type, which will provide an external IP address. Alternatively, for more complex routing, you can set up an Ingress resource.

Remember that exposing a pod directly might not be the best practice for production environments. It's usually better to expose a deployment or a set of pods using a service to ensure high availability and load balancing.


User Prompt
no there was another way

LLM Suite Response
If you're thinking of a way to quickly access a pod for testing or development purposes without setting up a service, you might be referring to using kubectl port-forward. This command allows you to forward a port from your local machine to a port on a pod. Here's how you can do it:

Port Forward to the Pod:

Use the kubectl port-forward command to forward a local port to a port on the pod. For example:

bash



kubectl port-forward pod/<pod-name> <local-port>:<pod-port>
Replace <pod-name> with the name of your pod, <local-port> with the port you want to use on your local machine, and <pod-port> with the port your application is listening on inside the pod.

Access the Pod via Browser:

Once the port forwarding is set up, you can access the application by navigating to http://localhost:<local-port> in your browser.

This method is particularly useful for development and debugging purposes, as it allows you to access a pod directly without modifying your cluster's network configuration. However, it's not suitable for production use, as it only works while the kubectl port-forward command is running and is limited to your local machine.







LLM Suite Response
To create an AWS Secrets Manager secret using Terraform, you can use the aws_secretsmanager_secret and aws_secretsmanager_secret_version resources. Below is an example of how you can define these resources in a Terraform configuration file:

hcl

provider "aws" {
  region = "us-west-2"  # Specify your desired AWS region
}

resource "aws_secretsmanager_secret" "example" {
  name        = "my_secret"
  description = "This is an example secret"
}

resource "aws_secretsmanager_secret_version" "example" {
  secret_id     = aws_secretsmanager_secret.example.id
  secret_string = jsonencode({
    username = "example_user"
    password = "example_password"
  })
}

output "secret_arn" {
  value = aws_secretsmanager_secret.example.arn
}
Explanation:
Provider Block: Specifies the AWS provider and the region where you want to create the secret.

aws_secretsmanager_secret Resource:

name: The name of the secret.
description: A description of the secret.
aws_secretsmanager_secret_version Resource:

secret_id: References the ID of the secret created by the aws_secretsmanager_secret resource.
secret_string: The actual secret data, encoded as a JSON string. In this example, it includes a username and password.
Output Block: Outputs the ARN of the created secret, which can be useful for referencing the secret in other parts of your infrastructure.

Usage:
Save the above code to a file, e.g., main.tf.
Initialize Terraform in your directory with terraform init.
Apply the configuration with terraform apply. You will be prompted to confirm the creation of the resources.
After applying, Terraform will output the ARN of the created secret.
Make sure to replace the example values with your actual secret data and adjust the AWS region as needed.










LLM Suite Response
To add a token to a Kubernetes ServiceAccount, you typically create a Secret that contains the token and then associate it with the ServiceAccount. Here's a step-by-step guide on how to do this:

Create a Secret with the Token:

First, you need to create a Kubernetes Secret that contains your token. You can do this by creating a YAML file for the secret or using the kubectl command directly.

Here's an example of creating a secret using a YAML file:

yaml



apiVersion: v1
kind: Secret
metadata:
  name: my-token-secret
type: Opaque
data:
  token: <base64-encoded-token>
Replace <base64-encoded-token> with your token encoded in base64. You can encode your token using a command like:

bash



echo -n "your-token" | base64
Apply the secret using:

bash



kubectl apply -f secret.yaml
Alternatively, you can create the secret directly using kubectl:

bash



kubectl create secret generic my-token-secret --from-literal=token=your-token
Associate the Secret with a ServiceAccount:

Once you have the secret, you need to associate it with a ServiceAccount. You can do this by modifying the ServiceAccount to reference the secret.

Here's an example of how to modify a ServiceAccount:

yaml



apiVersion: v1
kind: ServiceAccount
metadata:
  name: my-service-account
secrets:
  - name: my-token-secret
Apply the changes using:

bash



kubectl apply -f serviceaccount.yaml
Verify the Association:

You can verify that the secret is associated with the ServiceAccount by describing the ServiceAccount:

bash



kubectl describe serviceaccount my-service-account
You should see the secret listed under the Mountable secrets or Image pull secrets section, depending on how it's used.

By following these steps, you can add a token to a Kubernetes ServiceAccount by creating a secret and associating it with the ServiceAccount.



LLM Suite Response
Certainly! Let's simplify the tests while still covering the two main scenarios: updating the secret when it's empty and not updating when it already has a value. We'll focus on the core logic and reduce the complexity of the setup.

Here's a simplified version using pytest:

python



import pytest
from unittest.mock import patch, MagicMock
import json

# Assuming the script is saved in a file named 'vault_script.py'
from vault_script import update_secret, SECRET_NAME, SECRET_KEY

@pytest.fixture
def mock_boto_client():
    with patch('vault_script.boto3.client') as mock:
        yield mock

@pytest.fixture
def mock_requests():
    with patch('vault_script.requests.post') as mock_post, patch('vault_script.requests.get') as mock_get:
        yield mock_post, mock_get

def test_update_secret_success(mock_boto_client, mock_requests):
    mock_post, mock_get = mock_requests

    # Mock the POST request to Vault
    mock_post.return_value.json.return_value = {
        "auth": {"client_token": "mocked_hault_token"}
    }

    # Mock the GET request to Vault
    mock_get.return_value.json.return_value = {
        "data": {"DEFAULT_SERVICE_ACCOUNT_TOKEN": "mocked_service_account_token"}
    }

    # Mock the boto3 client and its methods
    mock_secrets_manager = MagicMock()
    mock_secrets_manager.get_secret_value.return_value = {
        'SecretString': json.dumps({SECRET_KEY: ""})
    }
    mock_boto_client.return_value = mock_secrets_manager

    # Call the function
    update_secret()

    # Assertions
    mock_secrets_manager.update_secret.assert_called_once_with(
        SecretId=SECRET_NAME,
        SecretString=json.dumps({SECRET_KEY: "mocked_service_account_token"})
    )

def test_update_secret_no_action_required(mock_boto_client, mock_requests):
    mock_post, mock_get = mock_requests

    # Mock the POST request to Vault
    mock_post.return_value.json.return_value = {
        "auth": {"client_token": "mocked_hault_token"}
    }

    # Mock the GET request to Vault
    mock_get.return_value.json.return_value = {
        "data": {"DEFAULT_SERVICE_ACCOUNT_TOKEN": "mocked_service_account_token"}
    }

    # Mock the boto3 client and its methods
    mock_secrets_manager = MagicMock()
    mock_secrets_manager.get_secret_value.return_value = {
        'SecretString': json.dumps({SECRET_KEY: "existing_token"})
    }
    mock_boto_client.return_value = mock_secrets_manager

    # Call the function
    update_secret()

    # Assertions
    mock_secrets_manager.update_secret.assert_not_called()
Key Points:
Fixtures: We use pytest.fixture to set up the mocks for boto3.client and requests calls. This keeps the test functions clean and focused on the logic being tested.

Test Functions:

test_update_secret_success: Verifies that the secret is updated when it is initially empty.
test_update_secret_no_action_required: Verifies that no update is performed when the secret already has a value.
Assertions: We check whether update_secret is called with the expected arguments or not called at all, depending on the scenario.

These tests focus on the core functionality and ensure that the script behaves correctly in both scenarios.



LLM Suite Response
To use a custom Certificate Authority (CA) and CA bundle with the requests library in Python, you can specify the verify parameter in your requests.post call. This parameter can be set to the path of a file containing your CA certificate or CA bundle. Here's how you can modify your code to include a custom CA or CA bundle:

Prepare Your CA Certificate or CA Bundle: Ensure you have your CA certificate or CA bundle file ready. This file should contain the PEM-encoded certificates.

Modify the Code: Update your requests.post call to include the verify parameter, pointing to your CA file.

Here's an example of how you can do this:

python



import requests

# Path to your CA certificate or CA bundle file
CA_CERT_PATH = "/path/to/your/ca-bundle.crt"

response = requests.post(
    f"{HAULT_URL}/v1/auth/kubernetes/login",
    json={"jwt": K8_TOKEN, "role": "vault-service-account-rotator-role"},
    timeout=30,
    verify=CA_CERT_PATH  # Use the custom CA certificate or bundle
)

# Check the response
if response.ok:
    print("Request was successful.")
else:
    print(f"Request failed with status code: {response.status_code}")
    print(f"Response: {response.text}")
Key Points
CA_CERT_PATH: Replace "/path/to/your/ca-bundle.crt" with the actual path to your CA certificate or CA bundle file.

Verify Parameter: By default, requests uses the system's trusted CA certificates. By specifying the verify parameter, you override this with your custom CA certificates.

Security: Ensure that the CA certificate or bundle you are using is trusted and secure, as it will be used to verify the server's SSL/TLS certificate.

Error Handling: Consider adding error handling to manage exceptions that might occur during the request, such as requests.exceptions.SSLError if there are issues with the SSL/TLS handshake.

By following these steps, you can securely make requests to a server using a custom CA or CA bundle with the requests library in Python.


User Prompt
how to decode ca.crt

LLM Suite Response
The ca.crt file is typically a PEM-encoded X.509 certificate, which is a text file containing base64-encoded data. You can decode and view the contents of this certificate using various tools. Here are a few methods to decode and inspect a ca.crt file:

Using OpenSSL
OpenSSL is a widely used tool for working with SSL/TLS certificates. You can use it to decode and display the contents of a certificate file.

Install OpenSSL: If you don't have OpenSSL installed, you can download and install it from OpenSSL's official website or use a package manager like apt, yum, or brew.

Decode the Certificate: Use the following command to decode and display the contents of the ca.crt file:

bash



openssl x509 -in ca.crt -text -noout
This command will output the details of the certificate, including the subject, issuer, validity period, and public key information.

Using Python
If you prefer to use Python, you can decode a PEM-encoded certificate using the cryptography library.

Install the Cryptography Library: If you don't have it installed, you can install it using pip:

bash



pip install cryptography
Decode the Certificate: Use the following Python script to read and decode the certificate:

python



from cryptography import x509
from cryptography.hazmat.backends import default_backend

# Load the certificate
with open("ca.crt", "rb") as cert_file:
    cert_data = cert_file.read()

# Parse the certificate
cert = x509.load_pem_x509_certificate(cert_data, default_backend())

# Print certificate details
print("Subject:", cert.subject)
print("Issuer:", cert.issuer)
print("Validity:")
print("  Not Before:", cert.not_valid_before)
print("  Not After:", cert.not_valid_after)
print("Serial Number:", cert.serial_number)
print("Public Key:", cert.public_key())
Using Online Tools
There are also online tools available that allow you to paste the contents of a PEM-encoded certificate and view its details. However, be cautious when using online tools, especially with sensitive certificates, as it may pose a security risk.

By using these methods, you can decode and inspect the contents of a ca.crt file to understand its details and verify its authenticity.



LLM Suite Response
It looks like you're trying to use OpenSSL to inspect or work with a CA (Certificate Authority) certificate file named ca.crt. Here are some common OpenSSL commands you can use to inspect the contents of a CA certificate:

View Certificate Details: To view the details of the certificate, such as the issuer, subject, validity period, and more, you can use the following command:

bash



openssl x509 -in ca.crt -text -noout
This command reads the ca.crt file and outputs the certificate details in a human-readable format.

Check Certificate Fingerprint: To get the fingerprint of the certificate, which is a unique identifier, you can use:

bash



openssl x509 -in ca.crt -noout -fingerprint
Verify Certificate: If you want to verify that the certificate is self-signed (i.e., it is a root CA certificate), you can use:

bash



openssl verify -CAfile ca.crt ca.crt
This command checks if the certificate can be verified against itself, which is typical for a root CA certificate.

Convert Certificate Format: If you need to convert the certificate from PEM to DER format, you can use:

bash



openssl x509 -in ca.crt -outform der -out ca.der
Conversely, to convert from DER to PEM:

bash



openssl x509 -in ca.der -inform der -out ca.crt -outform pem
These commands should help you inspect and work with your CA certificate using OpenSSL. If you have any specific tasks you need to perform with the certificate, feel free to ask!


User Prompt
can u use ? and : instead

LLM Suite Response
In Python, the ternary conditional operator uses the syntax x if condition else y, and it does not support the ? and : syntax that is used in languages like C, C++, and Java. Therefore, you cannot use ? and : for inline conditionals in Python. You should use the Python-specific syntax:

python



verify = CA_CERT_PATH if os == "dcore" else "skip" if os == "ncore" else None
This is the correct way to write an inline conditional expression in Python.


User Prompt
so is this correct:     try:
        response = requests.post(
            f"{HAULT_URL}/v1/auth/kubernetes/login",
            json={"jwt": K8_TOKEN, "role": "vault-service-account-rotator-role"},
            timeout=30,
            verify = CA_CERT_PATH if config.deploy_env != "dinfrc" else False,
        )

LLM Suite Response
Yes, your code snippet is mostly correct for using an inline conditional expression to set the verify parameter in the requests.post call. However, make sure that CA_CERT_PATH and config.deploy_env are defined in your code. Here's the snippet with a bit of formatting for clarity:

python



try:
    response = requests.post(
        f"{HAULT_URL}/v1/auth/kubernetes/login",
        json={"jwt": K8_TOKEN, "role": "vault-service-account-rotator-role"},
        timeout=30,
        verify=CA_CERT_PATH if config.deploy_env != "dinfrc" else False,
    )
In this code:

verify is set to CA_CERT_PATH if config.deploy_env is not equal to "dinfrc".
verify is set to False if config.deploy_env is equal to "dinfrc", which means that SSL certificate verification will be skipped.
Ensure that HAULT_URL, K8_TOKEN, CA_CERT_PATH, and config are properly defined and initialized in your code.


LLM Suite Response
To use pre-commit to lint check a specific file, you can follow these steps:

Install pre-commit: If you haven't already installed pre-commit, you can do so using pip:

bash



pip install pre-commit
Create a .pre-commit-config.yaml file: This file will define the hooks you want to use. Here is an example configuration that uses flake8 for linting Python files:

yaml



repos:
  - repo: https://github.com/pycqa/flake8
    rev: 6.0.0  # Use the latest stable version
    hooks:
      - id: flake8
Install the pre-commit hooks: Run the following command to install the hooks defined in your configuration file:

bash



pre-commit install
Run pre-commit on a specific file: You can manually run pre-commit on a specific file using the run command followed by the --files option. For example, to lint check a file named example.py, you would run:

bash



pre-commit run --files example.py
This command will execute the hooks specified in your .pre-commit-config.yaml file on the example.py file. You can replace example.py with the path to the file you want to check.

By following these steps, you can use pre-commit to lint check a specific file in your project.


LLM Suite Response
To run pre-commit with isort using the --profile black option from the command line without hardcoding it in the .pre-commit-config.yaml file, you can use the PRE_COMMIT_HOME environment variable to temporarily override the configuration. However, pre-commit itself doesn't directly support passing arguments from the command line to the hooks.

A common workaround is to temporarily modify the configuration file or use a script to automate this process. Here's a simple way to achieve this:

Create a temporary configuration file: You can create a script that modifies the .pre-commit-config.yaml file to include the desired arguments, runs pre-commit, and then restores the original configuration.

Script example:

bash



#!/bin/bash

# Backup the original configuration
cp .pre-commit-config.yaml .pre-commit-config.yaml.bak

# Modify the configuration to include the desired arguments
sed -i '/- id: isort/a\ \ \ \ \ \ \ \ args: [--profile, black]' .pre-commit-config.yaml

# Run pre-commit
pre-commit run --all-files

# Restore the original configuration
mv .pre-commit-config.yaml.bak .pre-commit-config.yaml
Run the script: Save the script to a file, for example, run_pre_commit.sh, and make it executable:

bash



chmod +x run_pre_commit.sh
Then run the script:

bash



./run_pre_commit.sh
This script temporarily adds the args: ['--profile', 'black'] to the isort hook in your .pre-commit-config.yaml, runs pre-commit, and then restores the original configuration. This way, you can run pre-commit with different arguments without permanently modifying your configuration file.


i have script A inside a pod in namespace 1 and want to execute it from another pod in namesapce B, what is the correct command for this?

LLM Suite Response
To execute a script from one pod in a different namespace on another pod, you can use kubectl exec with the appropriate namespace and pod name. Here’s a general approach to achieve this:

Identify the Pod and Namespace: Make sure you know the name of the pod and the namespace where the script resides.

Use kubectl exec: You can use kubectl exec to run commands inside a pod. However, since you want to execute a script from a pod in a different namespace, you will need to ensure that you have network access between the pods or use a service to expose the script.

Command Example: If you have network access and the script is accessible, you can use a command like this from the pod in namespace B:

bash



kubectl exec -n namespace-1 pod-name -- /path/to/script.sh
Replace namespace-1 with the actual namespace where the script is located, pod-name with the name of the pod containing the script, and /path/to/script.sh with the path to the script inside the pod.

Alternative Approach: If direct execution is not possible due to network policies or other restrictions, consider copying the script to the pod in namespace B using kubectl cp and then executing it locally.

Service Exposure: If the script needs to be accessed as a service, consider exposing it via a Kubernetes service and accessing it over HTTP or another protocol.

Make sure you have the necessary permissions and network policies in place to allow communication between the namespaces.


LLM Suite Response
In Kubernetes, you can't directly "mount" one deployment onto a job. However, you can share data between a deployment and a job using shared storage, such as a PersistentVolume (PV) and a PersistentVolumeClaim (PVC). This allows both the deployment and the job to access the same data.

Here's how you can set this up:

Create a PersistentVolume: This represents a piece of storage in your cluster.

Create a PersistentVolumeClaim: This is a request for storage by a pod. Both the deployment and the job will use the same PVC to access the shared storage.

Mount the PVC in both the Deployment and the Job: This allows both to read from and write to the same storage.

Example
Step 1: Define a PersistentVolume and PersistentVolumeClaim
yaml



# persistent-volume.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: example-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data

---

# persistent-volume-claim.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: example-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
Apply these configurations:

sh



kubectl apply -f persistent-volume.yaml
kubectl apply -f persistent-volume-claim.yaml
Step 2: Use the PVC in a Deployment
yaml



# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: example
  template:
    metadata:
      labels:
        app: example
    spec:
      containers:
      - name: app-container
        image: busybox
        command: ['sh', '-c', 'echo "Hello from Deployment" > /data/message.txt && sleep 3600']
        volumeMounts:
        - mountPath: /data
          name: example-volume
      volumes:
      - name: example-volume
        persistentVolumeClaim:
          claimName: example-pvc
Apply the deployment:

sh



kubectl apply -f deployment.yaml
Step 3: Use the PVC in a Job
yaml



# job.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: example-job
spec:
  template:
    spec:
      containers:
      - name: job-container
        image: busybox
        command: ['sh', '-c', 'cat /data/message.txt && sleep 10']
        volumeMounts:
        - mountPath: /data
          name: example-volume
      volumes:
      - name: example-volume
        persistentVolumeClaim:
          claimName: example-pvc
      restartPolicy: Never
Apply the job:

sh



kubectl apply -f job.yaml
Explanation
PersistentVolume (PV): Represents the actual storage. In this example, it's using hostPath for simplicity, but in a real-world scenario, you might use a cloud provider's storage solution.
PersistentVolumeClaim (PVC): A request for storage that can be used by pods.
Deployment and Job: Both mount the PVC at /data, allowing them to share files. The deployment writes a message to a file, and the job reads it.
This setup allows the deployment and the job to share data through the mounted volume.


export K8T=`cat /var/run/secrets/kubernetes.io/serviceaccount/token`;
export HAULT_TOKEN=`curl -k -s --request POST --data ' {"jwt": "'$K8T'", "role": "vault-operator-role"}' https://hault.105250-core-hault.svc.cluster.local:8200/v1/auth/kubernetes/login | jq -r ".auth.client_token"`;
export TOKEN=`curl -k -X GET -s -H "X-Vault-Token:$HAULT_TOKEN" "$HAULT_URL/v1/secret/dev/vault-auth-default-service-account" | jq -r '.data.DEFAULT_SERVICE_ACCOUNT_TOKEN'`;
curl -H "X-Auth-Token: $TOKEN"   -X GET "http://core-api-gateway.105250-core-vault.svc.cluster.local:8080/v1/service-accounts/3334187489314479372" 




Sedentary => 2000
Light active => 2300
High active => 2500

(temporal_workflow_endtoend_latency_seconds_sum{workflow_type=~"UKCreditCardEligibilityWorkflow|UKCreditCardApplyWorkflow"})


(temporal_activity_succeed_endtoend_latency_seconds_bucket{activity_type=~"SendApplyEligibilityResponse|SendCheckEligibilityResponse", workflow_type=~"UKCreditCardApplyWorkflow|UKCreditCardEligibilityWorkflow",task_queue="CREDIT_CARD_ELIGIBILITY_PUBLISH_RESPONSE_TASK_QUEUE"})


(temporal_workflow_completed_total{namespace=~"105250-borrow", exported_namespace="borrow", workflow_type=~"UKCreditCardApplyWorkflow|UKLoanEligibilityWorkflow"})


(temporal_workflow_endtoend_latency_seconds_count{workflow_type=~"UKCreditCardEligibilityWorkflow|UKCreditCardApplyWorkflow"})


Corebanking-temporal-client-metrics-sdk
Corebanking-temporal-server-common

========

apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: aws-secret-bearer-token
  namespace: monitoring
spec:
    provider: aws
    parameters:
        objects: |
            - objectName: "105250-0000ie-dcore-vault-monitoring"
              objectType: "secretsmanager"


args:
- '--bearer-token-file=/mnt/secrets-store/bearer-token'



- name: secrets-store-inline
  mountPath: "/var/run/secrets/kubernetes"
  readOnly: true


- name: secrets-store-inline
  csi:
    driver: secrets-store.csi.k8s.io
    readOnly: true
    volumeAttributes:
      secretProviderClass: "aws-secret-bearer-token"



- name: vault-prometheus-policy
  rules: |
    path "/sys/metrics" {
    capabilities = ["read"]
    }


{
  "Effect": "Allow",
  "Action": [
    "secretsmanager:GetSecretValue",
    "secretsmanager:DescribeSecret",
    "secretsmanager:List*"
    ],
    "Resource": ["arn:aws:secretsmanager:eu-west-1:${var.account_id}:secret:${var.seal_id}-${var.deployment_id}-${lower(var.environment)}-vault-monitoring-*"]
}

=========================================

-- Number of identity entities: avg(vault_identity_num_entities) => number of identity entities in HashiCorp Vault. In Vault, an identity entity is a unique identity that can be associated with one or more authentication methods. This allows for a unified identity management system within Vault. this is per each namespace. identity entity can be considered mix of user/identity and how it can be authenticated

-- Number of Leases: vault_expire_num_leases => the number of leases (a lease is a time-bound token or secret that has an expiration time. Leases are used to manage the lifecycle of secrets and tokens) that are currently managed by HashiCorp Vault

-- Tokens by TTL: avg without(instance) (vault_token_count_by_ttl) => the count of tokens in HashiCorp Vault, categorized by their time-to-live (TTL). TTL is a duration that specifies how long a token is valid before it expires.

-- Token Lookups: avg(irate(vault_token_lookup_count[1m])) => This metric likely represents the count of token lookup operations in HashiCorp Vault. A token lookup operation is typically used to retrieve information about a specific token, such as its policies, TTL, and other metadata, average by all vault pods

— avg(irate(vault_policy_get_policy_count[1m])) => count of policy retrieval operations in HashiCorp Vault. In Vault, policies are used to define what actions and operations are permitted for different users and roles. This metric would track the number of times policies have been retrieved or accessed.






kafka:
  panels:
    - name: fa_capquest_scod_updates_kafka_takes_too_long
      type: lag
      queries:
        - consumergroup: borrow-fa-gateway
          topics: 10x.tokenized.client-individual-customer-event-v001-002
          threshold: 0
          agg_function: min()
          agg_window: 5m
      alert:
        severity: MINOR
        for: 20m
        message: Consumer lag for tenx customer event topic in borrow-fa-gateway has been increasing for 20 minutes - fa-gateway is taking too long to process TenX events - customer details updates are not being sent over to Capquest in a timely manner
    - name: fa_capquest_pdd_updates_kafka_failure
      type: lag
      queries:
        - consumergroup: borrow-fa-gateway
          topics: chase.borrow.fa-gateway.client-individual-customer-event-v001.triage
          threshold: 0
          agg_function: min()
          agg_window: 15m
      alert:
        severity: MINOR
        for: 1m
        message: the consumer lag on this topic has been > 0 for the last 15 minutes - this means this event has now landed on the triage topic which has been blocked for 15 minutes - this indicates that a customer details update has failed to be sent over to Capquest in a timely manner
  global_tags:    
    grafana_notification_channel: Borrow-Collections-Squad
    customer_journey: BORROW-126



# 105250-0000ie-dinfrc-sa-token-rotator-tennessee-token
# https://eu-west-1.console.aws.amazon.com/secretsmanager/secret?name=105250-0000ie-dinfrc-sa-token-rotator-tennessee-token&region=eu-west-1

# 105250-0000ie-dinfrc-hault-unseal-keys => vault-root





arn:aws:sts::987705244904:assumed-role/105250-0000ie-dinfrc-vault-tools-role/botocore-session-1737725512  
s3:GetObject on resource: "arn:aws:s3:::105250-0000ie-dinfrc-hault-unseal-keys/'vault-test-old" with an explicit deny in a resource-based policy



init-containers:
  - name: vault-service-accounts-init
    image: {{ .Values.init.image }}/dynamo-services/icb-ledgers-vault-tools:{{ .Values.init.vaultToolsImageTag }}
    resources:
      requests:
        memory: "200Mi"
        cpu: 200m
      limits:
        memory: "1Gi"
    imagePullPolicy: "IfNotPresent"
    args:
      - "-ec"
      - python3 /scripts/sa-automate-dinfrc.py




curl -k "${REPOSTING_URL}:republish" -X POST -H "X-Auth-Token: ${REPOSTING_TOKEN}" -H "Content-Type: Application/Json" --data-binary "{\"account_id\": ,\"republish_type\": \"REPUBLISH_TYPE_REPUBLISH_BLOCKED_FAILURES\"}"




curl -k "${REPOSTING_URL}:republish" -X POST -H "X-Auth-Token: ${REPOSTING_TOKEN}" -H "Content-Type: Application/Json" \
    --data-binary "{\"account_id\": \"3be283e7-45f5-4207-ae98-6f73cd120ad6\",\"republish_type\": \"REPUBLISH_TYPE_REPUBLISH_BLOCKED_FAILURES\"}"



 State:          Running
      Started:      Thu, 25 Jul 2024 16:04:56 +0200
    Ready:          True
    Restart Count:  0
    Limits:
      cpu:     1800m
      memory:  6Gi
    Requests:
      cpu:        300m
      memory:     1536Mi



    Limits:
      cpu:     10m
      memory:  50Mi
    Requests:
      cpu:     10m
      memory:  50Mi


REVOKE EXECUTE ON FUNCTION pg_ls_waldir() FROM mycustomuser;
GRANT EXECUTE ON FUNCTION 
() TO <User>
GRANT EXECUTE ON FUNCTION pg_ls_waldir TO mycustomuser;


I used ALTER USER postgres_exporter WITH SUPERUSER; and it worked. After that, I also ran GRANT EXECUTE ON FUNCTION pg_ls_waldir() TO postgres_exporter;. Additionally, I learned that pg_monitor is the role that uses pg_ls_waldir, so either the user needs to be a superuser or granted the pg_monitor role.



metrics:
  collectors:
    wal: false


Hi, I think most probably the get_wal_size "helper" is missing. So you'd need to run the below SQL with superuser powers or actually the documentation even says that the built-in "pg_monitor" role and normal grants should work. But for non-superusers (the recommended way) you'd still need the helper.

CREATE OR REPLACE FUNCTION get_wal_size() RETURNS int8 AS
$$
select (sum((pg_stat_file('pg_wal/' || name)).size))::int8 from pg_ls_waldir()
$$ LANGUAGE sql VOLATILE SECURITY DEFINER;

GRANT EXECUTE ON FUNCTION get_wal_size() TO pgwatch2;
COMMENT ON FUNCTION get_wal_size() IS 'created for pgwatch2';


GRANT EXECUTE ON FUNCTION pg_ls_dir TO zabbix;


you need to grant execute on function pg_ls_dir instead of pg_ls_waldir


GRANT EXECUTE ON FUNCTION pg_read_binary_file(text,bigint,bigint,boolean) TO someuser;
GRANT EXECUTE ON FUNCTION pg_read_binary_file(text,bigint,bigint) TO someuser; 
GRANT EXECUTE ON FUNCTION pg_read_binary_file(text) TO someuser;


 name text, size bigint, modification timestamp with time zone )


scheduler@scheduler:[30512]:ERROR: permission denied for function pg_ls_waldir


FROM dockerdev.artifactory.dyn.prd.aws.jpmchase.net/container-external/docker.external.thoughtmachine.io/vault/installer/operators/crown-operator@sha256:d0ef181b3888c895a11d638b258d2c7ead92dea009c871bc0447108b4dc07301
ENV NO_PROXY="sts.eu-west-1.amazonaws.com,.svc,svc.cluster.local,0.0.0.0/0"
ENV HTTPS_PROXY="http://proxy.jpmchase.net:10443"
ENV HTTP_PROXY="http://proxy.jpmchase.net:10443"
ENV PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/deployment-tools"

USER root
SHELL ["sh", "-c"]
COPY JPMCROOTCA.pem /usr/local/share/ca-certificates/JPMCROOTCA.crt
RUN update-ca-certificates || true
USER tm-app



kafka:
  panels:
    - name: fa_capquest_transaction_update_kafka_takes_too_long_ODS
      type: lag
      queries:
        - consumergroup: borrow-fa-gateway
          topics: chase.ods-transaction-enrichment-service.transaction-enrichment-event-v001
          threshold: 0
          agg_function: min()
          agg_window: 5m
      alert:
        severity: MINOR
        for: 20m
        message: Consumer lag for transaction event topic in borrow-fa-gateway has been increasing for 20 minutes - fa-gateway is taking too long to process ODS transaction events - customer transactions are not being sent over to Capquest in a timely manner
    - name: fa_capquest_transaction_update_kafka_failure_process_ODS
      type: lag
      queries:
        - consumergroup: borrow-fa-gateway
          topics: chase.borrow.fagateway.transaction-enrichment-event-v001.triage
          threshold: 0
          agg_function: min()
          agg_window: 15m
      alert:
        severity: MINOR
        for: 1m
        message: the consumer lag on this topic has been > 0 for the last 15 minutes - this means this event has now landed on the triage topic which has been blocked for 15 minutes - this indicates that a transaction update has failed to be sent over to Capquest in a timely manner
  global_tags:    
    grafana_notification_channel: Borrow-Collections-Squad
    customer_journey: BORROW-122




slo:
  name: account_balance_error_rate
  sli:
    events:
      sli_error_query: sum(rate(http_server_requests_seconds_count{app="borrow-balance-service", uri=~"/api/v1/borrow/(accounts/live-balances|internal/live-balance-views|live-balance-views|accounts/timerange-balances)", method="GET", status!~"2.*|3.*"}[{{ window }}])) or vector(0)
      sli_total_query: sum(rate(http_server_requests_seconds_count{app="borrow-balance-service", uri=~"/api/v1/borrow/(accounts/live-balances|internal/live-balance-views|live-balance-views|accounts/timerange-balances)", method="GET"}[{{ window }}]))
  objective: 0.99
  labels:
    service: borrow_balance_service
    squad: core_processing
    source: internal
    domain: borrow
    customer_journey: BORROW-042
  alerts:
    dry_run_mode: True
  grafana_notification_channel: SRE_Core-Alertforge




kafka:
  panels:
    - name: fa_capquest_customer_deceased_update_kafka_takes_too_long_tenx
      type: lag
      queries:
        - consumergroup: borrow-fa-gateway
          topics: 10x.tokenized.client-individual-customer-event-v001-002
          threshold: 0
          agg_function: min()
          agg_window: 5m
      alert:
        severity: MINOR
        for: 20m
        message: Consumer lag for tenx customer event topic in borrow-fa-gateway has been increasing for 20 minutes - fa-gateway is taking too long to process Tenx events - customer deceased updates are not being sent over to Capquest in a timely manner
    - name: fa_capquest_transaction_update_kafka_failure_tenx
      type: lag
      queries:
        - consumergroup: borrow-fa-gateway
          topics: chase.borrow.fa-gateway.client-individual-customer-event-v001.triage
          threshold: 0
          agg_function: min()
          agg_window: 15m
      alert:
        severity: MINOR
        for: 1m
        message: the consumer lag on this topic has been > 0 for the last 15 minutes - this means that this event has now landed on the triage topic which has been blocked for 15 minutes - this indicates that a customer deceased update has failed to be sent over to Capquest in a timely manner
  global_tags:    
    grafana_notification_channel: Borrow-Collections-Squad
    customer_journey: BORROW-123
    additional_journeys: BORROW-126



kafka:
  panels:
    - name: fa_capquest_customer_informed_fin_diff_onboarding_kafka_failures_inprocessing
      type: lag
      queries:
        - consumergroup: borrow-collections-service
          topics: chase.borrow.collections.client-individual-customer-event-v001.triage
          threshold: 0
          agg_function: min()
          agg_window: 10m
      alert:
        severity: MINOR
        for: 1m
    - name: fa_capquest_customer_informed_fin_diff_onboarding_kafka_failures_inprocessing
      type: lag
      queries:
        - consumergroup: borrow-fa-gateway
          topics: chase.borrow.fa-gateway.collections-events-v003.triage
          threshold: 0
          agg_function: min()
          agg_window: 10m
      alert:
        severity: MINOR
        for: 1m
    - name: fa_capquest_customer_informed_fin_diff_onboarding_kafka_takes_too_long
      type: lag
      queries:
        - consumergroup: borrow-collections-service
          topics: 10x.tokenized.client-individual-customer-event-v001-002
          threshold: 0
          agg_function: min()
          agg_window: 5m
      alert:
        severity: MINOR
        for: 10m
        message: There has been consumer lag on tenx event topic in collections-service for over 10 mins - collections-service is taking too long to process TenX events - customer informed fin diff onboarding is not happening quickly enough
    - name: fa_capquest_customer_informed_fin_diff_onboarding_kafka_takes_too_long_process_collection
      type: lag
      queries:
        - consumergroup: borrow-fa-gateway
          topics: chase.borrow.collections-events-v003
          threshold: 0
          agg_function: min()
          agg_window: 5m
      alert:
        severity: MINOR
        for: 10m
        message: There has been consumer lag in fa-gateway for collections event topic for over 10 minutes - fa-gateway is taking too long to process collections outbox events from collections-service - customer informed fin diff onboarding is not happening fast enough
  global_tags:    
    grafana_notification_channel: Borrow-Collections-Squad
    customer_journey: BORROW-113



slo:
  name: fa_capquest_vulnerability_latency
  sli:
    events:
      sli_error_query: sum(rate(http_server_requests_seconds_count{app="borrow-fa-gateway", uri="/api/v1/borrow/financial-assistance/gateway/customer/{customerId}/status/{statusId}", method="PUT", status=~"2.*"}[{{ window }}])) - sum(rate(http_server_requests_seconds_bucket{app="borrow-fa-gateway", uri="/api/v1/borrow/financial-assistance/gateway/customer/{customerId}/status/{statusId}", method="PUT", status=~"2.*", le=~"8.*"}[{{ window }}]))
      sli_total_query: sum(rate(http_server_requests_seconds_count{app="borrow-fa-gateway", uri="/api/v1/borrow/financial-assistance/gateway/customer/{customerId}/status/{statusId}", method="PUT", status=~"2.*"}[{{ window }}]))
  objective: 0.99
  labels:
    service: borrow-fa-gateway
    squad: core_processing
    source: internal
    domain: borrow
    customer_journey: BORROW-109
  alerts:
    dry_run_mode: True
  grafana_notification_channel: SRE_Core-Alertforge






FROM dockerprod.artifactory.dyn.prd.aws.jpmchase.net/container-base/managedbaseimages/python:3.10.7-stable AS base
# Setup env
ENV LANG C.UTF-8
ENV LC_ALL C.UTF-8
ENV PYTHONDONTWRITEBYTECODE 1
ENV PYTHONFAULTHANDLER 1

FROM base AS python-deps

RUN pip config set global.index-url https://artifactory.dyn.prd.aws.jpmchase.net/artifactory/api/pypi/pypidev/simple/
RUN pip config set global.trusted-host https://artifactory.dyn.prd.aws.jpmchase.net/artifactory/api/pypi/pypidev/simple/

RUN pip install pipenv

COPY Pipfile .
COPY Pipfile.lock .
COPY Standard_CA_Roots.pem /usr/local/share/
ENV PIP_CERT=/usr/local/share/Standard_CA_Roots.pem
RUN PIPENV_VENV_IN_PROJECT=1 pipenv install --dev --deploy

FROM base AS runtime

# Copy virtual env from python-deps stage
COPY --from=python-deps /.venv /.venv
ENV PATH="/.venv/bin:$PATH"

RUN mkdir -p /opt/app/

COPY . /opt/app/soteria
RUN chown -R jpmcnobody /opt/app/
RUN chmod -R 755 /opt/app
USER 999
WORKDIR /opt/app/soteria


MakeFile
INSTALL_STAMP := .install.stamp
PIPENV := $(shell command -v pipenv 2> /dev/null)

.DEFAULT_GOAL := help

.PHONY: help
help:
		@echo "Please use 'make <target>' where <target> is one of"
		@echo ""
		@echo "  install     install packages and prepare environment"
		@echo "  clean       remove all temporary files"
		@echo "  lint        run the code linters"
		@echo "  format      reformat code"
		@echo "  test        run all the tests"
		@echo ""
		@echo "Check the Makefile to know exactly what each target is doing."

install: $(INSTALL_STAMP)
$(INSTALL_STAMP): Pipfile.lock
		@if [ -z $(PIPENV) ]; then echo "Pipenv could not be found. See https://pipenv.pypa.io/"; exit 2; fi
		$(PIPENV) install --dev
		touch $(INSTALL_STAMP)

.PHONY: clean
clean:
		find . -type d -name "__pycache__" | xargs rm -rf {};
		rm -rf $(INSTALL_STAMP)

.PHONY: lint
lint: $(INSTALL_STAMP)
		$(PIPENV) run isort --profile=black --check-only ./tests/ ./src/
		$(PIPENV) run black --check ./tests/ ./src/ --diff
		$(PIPENV) run mypy ./src/ ./tests/
		$(PIPENV) run flake8 ./src/ ./tests/

.PHONY: format
format: $(INSTALL_STAMP)
		$(PIPENV) run isort --profile=black ./tests/ ./src/
		$(PIPENV) run black ./tests/ ./src/

.PHONY: test
test: $(INSTALL_STAMP)
		$(PIPENV) run pytest ./tests/ --cov-report term-missing --cov-fail-under 70 --cov ./src/

.PHONY: py-start
py-start:
		$(PIPENV) run python3 main.py -e dev -sclist config/infra/core/eks/scenarios/eks_blackhole_az_attack.yaml



Pipfile
[[source]]
url = "https://artifacts-read.gkp.jpmchase.net/artifactory/api/pypi/pypi/simple/"
verify_ssl = true
name = "pip_conf_index_:env:"

[packages]
pyyaml = "*"
jinja2 = "*"
gitpython = "*"
requests = "*"
pytz = "*"
mypy-boto3-dynamodb = "*"
annotated-types = "*"
types-pytz = "*"
types-PyYAML = '*'
selenium = "*"
gitdb = "*"
boto3 = "*"
boto3-stubs = "*"
botocore = "*"
aws-secretsmanager-caching = "*"

[dev-packages]
isort = "*"
pre-commit = "*"
black = "*"
pytest = "==8.0.2"
flake8 = "*"
mypy = "*"
coverage = "*"
pytest-cov = "*"
types-requests = "*"
moto = {version = "==5.0.2", extras = ["dynamodb"]}

[requires]
python_version = "3.10"



setup.cfg
[flake8]
extend-ignore = E501, W503, E203
[mypy]
ignore_missing_imports = True




import json
import logging
import os
import re
import shutil
from datetime import datetime, timezone
from logging import getLogger

import botocore
import botocore.session
import yaml
from aws_secretsmanager_caching import SecretCache, SecretCacheConfig
from botocore.exceptions import ClientError

from src.api.selenium_client import Selenium

LOGGER = getLogger(__name__)
SECRET_DATA = None


def parse_yaml_file(yaml_file_path):
    with open(yaml_file_path, "r") as yaml_file:
        yaml_data = yaml.safe_load(yaml_file)
    return yaml_data


def create_or_clean_screenshot_folder(folder):
    try:
        os.mkdir(folder)
    except FileExistsError:
        folder_cleanup(folder)
        LOGGER.debug(f"Target folder already exist {folder}")


def folder_cleanup(folder):
    for filename in os.listdir(folder):
        file_path = os.path.join(folder, filename)
        try:
            if os.path.isfile(file_path) or os.path.islink(file_path):
                os.unlink(file_path)
            elif os.path.isdir(file_path):
                shutil.rmtree(file_path)
        except Exception as e:
            LOGGER.debug("Failed to delete %s. Reason: %s" % (file_path, e))


async def get_other_screenshots(url, health_check):
    try:
        with Selenium(base_url=f"{url}") as gs:
            for hc in health_check.values():
                gs.get_screenshot(hc, (url + hc).replace("/", "-"))
    except Exception as e:
        LOGGER.critical(f"Screenshot Capturing Failed, Error: {e}")
        raise e


def check_if_file_exist(path):
    if not os.path.exists(path):
        LOGGER.critical(
            f"File {path} not exists, please check documentation and creat."
        )
        exit()


def write_config_file(CONFIG_FILE_PATH, parameter, value):
    try:
        with open(CONFIG_FILE_PATH, "r+") as file:
            config = json.load(file)
            config[parameter] = value
            file.seek(0)
            json.dump(config, file, indent=4)
            file.truncate()
            LOGGER.debug(f"Writing {parameter} to {CONFIG_FILE_PATH}")
    except FileNotFoundError:
        with open(CONFIG_FILE_PATH, "w") as file:
            json.dump({parameter: value}, file, indent=4)
            LOGGER.debug(f"Created {CONFIG_FILE_PATH} and writing {parameter}")


# Function to read a parameter's value from the config file
def read_config_file(CONFIG_FILE_PATH, parameter):
    try:
        with open(CONFIG_FILE_PATH, "r") as file:
            config = json.load(file)
            LOGGER.debug(f"Parameter {parameter} read from {CONFIG_FILE_PATH}")
            return config.get(parameter)
    except FileNotFoundError:
        LOGGER.info(f"Config file {CONFIG_FILE_PATH} not found")
        return None


def url_environment_replacer(url: str, link: str) -> str:
    if "environment" in url:
        try:
            LOGGER.debug(
                f"Replaced healthcheck environment in link {url}, by target {link}"
            )
            url = re.sub(r"environment", link, url)
        except KeyError as e:
            LOGGER.warning(
                f"Unknown url:link mapping {e}. Please check {url} and mapping in consts.py"
            )
    LOGGER.warning(
        f"Cannot find environment in link, please check configs, fallback to provided link {url}"
    )
    return url


def transform_time(time_stamp, structure="short"):
    time_stamp = int(time_stamp) / 1000
    dt_object = datetime.fromtimestamp(time_stamp, timezone.utc)
    if structure == "long":
        return dt_object.strftime("%Y-%m-%d-%H:%M:%S")
    return dt_object.strftime("%H:%M:%S")


def all_experiments_start_end(data):
    if "start" in data and "end" in data:
        lowest_start = data["start"]
        biggest_end = data["end"]
    else:
        biggest_end = max(event["end"] for event in data.values())
        lowest_start = min(event["start"] for event in data.values())
    return f"S_{transform_time(lowest_start, structure='long')}-E_{transform_time(biggest_end, structure='long')}"


def get_secret(secret_name, region_name):
    logging.getLogger("botocore").setLevel(logging.ERROR)
    try:
        # Create a Secrets Manager client
        client = botocore.session.get_session().create_client(
            "secretsmanager", region_name=region_name
        )
        LOGGER.info(f"Fetching the details for the secret name {secret_name}")
        cache_config = SecretCacheConfig()
        cache = SecretCache(config=cache_config, client=client)
        secret = cache.get_secret_string(secret_name)
        secret_data = json.loads(secret)
        return secret_data
    except ClientError as e:
        LOGGER.error(f"Exception details: {e}")
        # For a list of exceptions thrown, see
        # https://docs.aws.amazon.com/secretsmanager/latest/apireference/API_GetSecretValue.html
        raise e


def kill_selenium_processes(process):
    proc_dir = "/proc"
    LOGGER.debug(f"Attempt to kill selenium PID name {process}")
    for pid_dir in os.listdir(proc_dir):
        if pid_dir.isdigit():
            cmdline_path = os.path.join(proc_dir, pid_dir, "cmdline")
            try:
                if os.path.isfile(cmdline_path):
                    with open(cmdline_path) as f:
                        cmdline = f.read()
                        if process in cmdline:
                            os.kill(int(pid_dir), 9)
                            LOGGER.info(
                                f"Killed process {pid_dir} with cmdline containing {process}"
                            )
            except Exception as e:
                LOGGER.error(f"Error processing PID {pid_dir}: {e}")



import logging
import os

from src import consts
from src.common import parse_yaml_file


class ConfigBuild:
    def __init__(self):
        self.logger = logging.getLogger(__name__)
        self.config_dir = consts.CONFIG_DIR
        self.config_directory = self.config_directory_check()

    def config_directory_check(self):
        """Find and validate config directory"""
        try:
            if not os.path.isdir(self.config_dir):
                parent_dir = os.path.abspath(
                    os.path.join(os.path.dirname(__file__), "..")
                )
                config_dir = os.path.join(parent_dir, self.config_dir)
                os.chdir(config_dir)
                os.chdir(parent_dir)
                return config_dir
            else:
                return os.path.abspath(self.config_dir)
        except FileNotFoundError as e:
            self.logger.error(
                f"Config directory not found: {self.config_dir}, Error: {e}"
            )
            raise e

    def generate_config_files_list(self):
        """List of Config Files generated from scenarios mapping"""
        try:
            config_files_list = [
                os.path.join(root, directory, file)
                for root, dirs, files in os.walk(self.config_directory)
                for directory in dirs
                if directory == "scenarios"
                for file in os.listdir(os.path.join(root, str(directory)))
            ]
            return config_files_list
        except Exception as e:
            self.logger.critical(f"Config files list generation failed, Error: {e}")
            raise e

    def parse_config_files(self, file_list):
        """Parse input config files and build the config dictionary dynamically"""
        config_dict = {}
        try:
            for config_file in file_list:
                config_file_data = parse_yaml_file(config_file)
                acc = config_file_data["targets"].split("/")[2]
                runenv = config_file_data["targets"].split("/")[1]
                dict_key = f"{runenv}_{acc}_{config_file_data['attacks_dictionary'].split('/')[-1].split('.')[0]}"
                config_dict.setdefault(dict_key, {})
                config_dict[dict_key]["runenv"] = runenv

                for key, val in config_file_data.items():
                    if key == "attacks_dictionary" or key == "targets":
                        config_dict[dict_key][key] = parse_yaml_file(val)
                    elif key == "health_checks":
                        self.parse_health_checks(config_dict, dict_key, key, val)
            return config_dict

        except Exception as e:
            self.logger.critical(f"Config files parsing failed, Error: {e}")
            raise e

    def parse_health_checks(self, config_dict, dict_key, key, val) -> dict:
        """Parse the health checks and returns the list of dashboards and customer journey details"""
        try:
            dbd_yaml_list: list = []
            config_dict[dict_key][key] = val
            for hc_ip in val:
                if os.path.isdir(hc_ip):
                    dbd_yaml_list.extend(
                        os.path.join(hc_ip, file) for file in os.listdir(hc_ip)
                    )
                else:
                    dbd_yaml_list.append(hc_ip)
            config_dict[dict_key][key] = [
                parse_yaml_file(yaml_read) for yaml_read in dbd_yaml_list
            ]
            return config_dict

        except Exception as e:
            self.logger.critical(
                f"Config health check files parsing failed, Error: {e}"
            )
            raise e



import json
import logging
import re
import time
import urllib.parse
from datetime import datetime

import requests

from db.dynamodb import DynamoDbService
from src import consts
from src.api.grafana_api import GrafanaApi
from src.api.selenium_client import Selenium
from src.common import get_secret, parse_yaml_file, transform_time

LOGGER = logging.getLogger(__name__)
dynamodb_service = DynamoDbService()


def get_panel_name(url, dashboard, panel):
    if consts.IS_CLOUD:
        api_tokens = parse_yaml_file(consts.GRAFANA_API_KEYS_PATH)
        secret_api_key = get_secret(consts.SECRETS_NAME, consts.REGION_NAME)
        grafana_secrets_name = api_tokens[url]
        grf_obj = GrafanaApi(
            api_token=secret_api_key[grafana_secrets_name], base_url=url
        )
    else:
        api_tokens = parse_yaml_file(consts.GRAFANA_API_KEYS_PATH_LOCAL)
        grf_obj = GrafanaApi(api_token=api_tokens[url], base_url=url)
    dash_data = grf_obj.get_dashboard(uid=dashboard).json()
    panel_id = re.findall(r"viewPanel=(\d+)", panel)
    dashboard_name = dash_data.get("dashboard", {}).get("title", "Unknown_name")
    if panel_id:
        for panel in dash_data["dashboard"]["panels"]:
            if "panels" in panel:
                for child_panel in panel["panels"]:
                    if child_panel["id"] == int(panel_id[0]):
                        return dashboard_name + "-" + child_panel["title"]
                    elif panel["id"] == panel_id:
                        return dashboard_name + "-" + panel["title"]
        return dashboard_name + "panel_id_" + panel_id[0]
    return dashboard_name


def tab_handler(panel, new_dash_dict, dash_uid, hc, gs):
    try:
        try:
            panel_info = f"?{panel.split('?')[1]}"
        except IndexError:
            panel_info = ""
        range_filter = f"&from=now-{consts.TIME_RANGE}h&to=now"
        new_url = new_dash_dict[dash_uid]
        joined_url = new_url + panel_info
        if "orgId" not in joined_url:
            joined_url = joined_url + "?orgId=1"

        url_result = urllib.parse.urlparse(joined_url)
        panel_query = url_result.query
        window_height = hc.get("windows_height", consts.WINDOW_HEIGHT_DEFAULT)
        if panel_query.__contains__("from"):
            suffix = f"&{panel_query.split('&')[-2]}&{panel_query.split('&')[-1]}"
            updated_url = joined_url.removesuffix(suffix)
            dash_url_with_range = f"{updated_url}{range_filter}"
            gs.open_new_tab(dash_url_with_range, window_height)
        else:
            dash_url_with_range = f"{joined_url}{range_filter}"
            gs.open_new_tab(dash_url_with_range, window_height)
    except (KeyError, json.decoder.JSONDecodeError) as e:
        LOGGER.warning(f"f(Cannot capture panel {panel}), {e}")
    except Exception as e:
        LOGGER.critical(f"Unknown error, appeared during capture {e}")
        raise e


async def get_grafana_results(
    dash_list, grafana_api_url, health_checks, scenario_to_annotate
):
    get_metadata = dynamodb_service.get_item(
        table_name="soteria_metadata",
        primary_key={"scenario_id": scenario_to_annotate["scenario_id"]},
    )
    new_dash_dict = {}
    screenshot_names = []
    with Selenium(
        base_url=f"{grafana_api_url}/d/", screenshot_folder=consts.SCREENSHOT_FOLDER
    ) as gs:
        gs.graphana_auth()
        for dbd_list in dash_list:
            for key, val in dbd_list.items():
                new_dash_dict[key] = val["url"]
        for hc in health_checks:
            dash_uid_list = hc["dashboards"]
            for dash_uid, panels in dash_uid_list.items():
                for panel in panels:
                    LOGGER.debug(f"Starting Selenium for panel {panel}")
                    tab_handler(panel, new_dash_dict, dash_uid, hc, gs)
                    start_ = get_metadata["scenario_metadata"]["start"]
                    end_ = get_metadata["scenario_metadata"]["end"]
                    screenshot_names.append(
                        f"{get_panel_name(url=grafana_api_url, dashboard=dash_uid, panel=panel)}-"
                        f"S_{transform_time(start_)}-"
                        f"E_{transform_time(end_)}"
                    )
            time.sleep(consts.SCREENSHOT_DELAY)
            gs.get_screenshots(screenshot_names=screenshot_names)
        gs.driver_quit()


class Grafana:
    def __init__(self, grafana_api_url, api_token) -> None:
        self.logger = logging.getLogger(__name__)
        self.grafana_api_url = grafana_api_url
        self.ga = GrafanaApi(api_token=api_token, base_url=grafana_api_url)
        self.did_list: list = []
        self.annotation_id = 0

    def build_dashboard_json(self, conf, dashboard_dict, scenario_name, folder_id=0):
        try:
            seal_id = ""
            if conf == "product":
                seal_id = 105250
            elif conf == "infra":
                seal_id = ""
            time = datetime.now()
            d1 = time.strftime("%Y-%m-%d-%H:%M:%S")
            title = dashboard_dict["dashboard"]["title"]
            dash_mod_name = f"{d1}-{title}-{seal_id}-{conf}-Gremlin-{scenario_name}"
            dashboard_dict["meta"] = None
            dashboard_dict["dashboard"]["title"] = dash_mod_name
            dashboard_dict["dashboard"]["id"] = None
            dashboard_dict["dashboard"]["uid"] = None
            dash_payload = dashboard_dict["dashboard"]
            data = {
                "dashboard": dash_payload,
                "folderId": folder_id,
                "overwrite": False,
            }
            return json.dumps(data, indent=4)
        except Exception as e:
            self.logger.critical(f"Build Dashboard json failed, Error: {e}")
            raise e

    def get_dash_uid_by_name(self, dashboard_dict, folder_id):
        list_of_all_dashboard = self.ga.scan_folder(folder_id).json()
        for dash in list_of_all_dashboard:
            title = dash["title"]
            if title == dashboard_dict["dashboard"]["title"]:
                LOGGER.warning(
                    f"The dashboard was copied with an error, but the copy exists in Grafana. "
                    f"Please check collected screenshots for the {title}"
                )
                return dash
        else:
            LOGGER.critical(
                f"Cannot copy dashboard {dashboard_dict['dashboard']['title']}. Exiting"
            )
            exit()

    def generate_dashboard(self, folder_id, health_checks, conf, scenario_name):
        for dashboard in health_checks:
            dash_uid_list = dashboard["dashboards"].keys()
            for dash_uid in dash_uid_list:
                try:
                    res = self.ga.get_dashboard(dash_uid)
                    dashboard_dict = json.loads(res.text)
                    dashboard_input_json = self.build_dashboard_json(
                        conf, dashboard_dict, scenario_name, folder_id
                    )
                    res = self.ga.create_dashboard(dashboard_input_json)
                    if res.status_code != requests.codes.ok:
                        LOGGER.debug(
                            f"Grafana return {res.status_code}, fallback to "
                            "checking if the target dashboard copied with the error"
                        )
                        r = self.get_dash_uid_by_name(dashboard_dict, folder_id)
                    else:
                        r = json.loads(res.text)
                    custom = {dash_uid: r}
                    self.did_list.append(custom)
                except Exception as e:
                    self.logger.warning(
                        f"Creation of the dashboard failed, Error: {e}, skipping {dash_uid}"
                    )
        return self.did_list

    def create_annotation(self, run_time, ann_tags):
        try:
            get_metadata = dynamodb_service.get_item(
                table_name="soteria_metadata",
                primary_key={"scenario_id": run_time["scenario_id"]},
            )
            start_time = get_metadata["scenario_metadata"]["start"]
            end_time = get_metadata["scenario_metadata"]["end"]
            data = {
                "time": int(start_time),
                "timeEnd": int(end_time),
                "tags": ann_tags,
                "text": "Annotation for Soteria Chaos Automation",
            }
            input_data = json.dumps(data)
            re_annotation = self.ga.create_annotation(input_data)
            self.annotation_id = re_annotation.json()["id"]
        except Exception as e:
            self.logger.critical(f"Create annotation failed, Error: {e}")
            raise e

    def delete_annotation(self):
        try:
            self.ga.delete_annotation(self.annotation_id)
        except Exception as e:
            self.logger.critical(
                f"Cannot remove annotation {self.annotation_id}, Error: {e}"
            )

    def get_dashboard_uid(self, dash_list) -> list:
        try:
            dash_uid_list = [
                val["uid"] for dash_uid in dash_list for key, val in dash_uid.items()
            ]
            return dash_uid_list
        except Exception as e:
            self.logger.critical(f"Dashboard uid retrieval failed, Error: {e}")
            raise e

    def build_annotation_json(self, conf, gc, res_dict, dash_title):
        try:
            if re.search(conf, dash_title):
                input_conf = gc
                annotation_list = res_dict["dashboard"]["annotations"]["list"]
                tags = [hc["tags"] for hc in input_conf]
                if len(annotation_list) > 1:
                    for i in annotation_list:
                        if "builtIn" not in i:
                            i["tags"] = tags
                else:
                    input_dict = {
                        "datasource": "-- Grafana --",
                        "enable": True,
                        "hide": True,
                        "iconColor": "#FADE2A",
                        "limit": 100,
                        "matchAny": True,
                        "name": "soteria",
                        "showIn": 0,
                        "tags": tags,
                        "type": "tags",
                    }
                    annotation_list.append(input_dict)
        except Exception as e:
            self.logger.critical(f"Build annotation json failed, Error: {e}")
            raise e

    def inject_annotation(self, dash_list, folder_id, k, v):
        try:
            for dash_uid in self.get_dashboard_uid(dash_list):
                result = self.ga.get_dashboard(dash_uid)
                res_dict = json.loads(result.text)
                dash_title = res_dict["dashboard"]["title"]
                self.logger.debug("Creating annotation")
                self.build_annotation_json(k, v, res_dict, dash_title)
                data = {
                    "dashboard": res_dict["dashboard"],
                    "folderId": folder_id,
                    "overwrite": True,
                }
                dashboard_input_json = json.dumps(data, indent=4)
                self.ga.update_dashboard(dashboard_input_json)
        except Exception as e:
            self.logger.critical(f"Inject annotation failed, Error: {e}")
            raise e

    def cleanup_soteria_dashboards(self):
        for dashboard in self.did_list:
            try:
                dash_uid = next(iter(dashboard.values()))["uid"]
                self.ga.delete_dashboard(dash_uid)
            except Exception as e:
                self.logger.warning(f"Cannot remove dashboard {e}")


if __name__ == "__main__":
    logging.basicConfig(format="%(asctime)s %(message)s", level=logging.DEBUG)




import datetime
import os
from logging import getLogger

import boto3
from botocore.exceptions import NoCredentialsError

from src import consts

LOGGER = getLogger(__name__)


def upload_files_to_s3(
    attack_target,
    source_folder_path=consts.SCREENSHOT_FOLDER,
    bucket_name=consts.BUCKET_NAME,
    target_folder=None,
):
    """
    Uploads the contents of a directory to an S3 bucket, preserving the folder structure.

    :param attack_target:
    :param source_folder_path:
    :param bucket_name:
    :param target_folder:
    """
    # Create an S3 client
    s3 = boto3.client("s3")

    if target_folder is None:
        target_folder = (
            attack_target + "-" + datetime.datetime.now().strftime("%Y-%m-%d %H-%M")
        )
    # Walk through all the files and subdirectories in the local directory
    for root, dirs, files in os.walk(source_folder_path):
        for file_name in files:
            # Get the full path of the file
            file_path = os.path.join(root, file_name)

            # Construct the key for S3, by removing the base directory and preserving folder structure
            relative_path = os.path.relpath(file_path, source_folder_path)
            s3_key = os.path.join(target_folder, relative_path)

            try:
                # Upload the file to S3 bucket
                s3.upload_file(file_path, bucket_name, s3_key)
                LOGGER.info(f"File '{file_name}' uploaded to '{bucket_name}/{s3_key}'")
            except NoCredentialsError:
                LOGGER.critical("AWS credentials not available.")
                return
            except Exception as e:
                LOGGER.critical(f"Error uploading file '{file_name}': {str(e)}")
        return f"s3://{consts.BUCKET_NAME}/{s3_key.split('/')[0]}"


#!/usr/bin/env bash
for NS in $(kubectl get ns|grep -i '105250-core'|awk '{print $1}')
do
    for TYPE in configmap services deployments statefulset horizontalpodautoscalers cronjob virtualservices ingresses
    do
        echo "Grabbing $NS $TYPE"
        mkdir -p $NS/$TYPE
        for ENTITY in $(kubectl -n $NS get $TYPE| grep -v NAME| awk '{print $1}')
        do
            kubectl -n $NS get $TYPE $ENTITY -o yaml > $NS/$TYPE/$ENTITY.yaml
        done
    done
done





---
apiVersion: v1
kind: Pod
metadata:
  name: vault-installer-105250-core-vault
  namespace: 105250-core-vault
  labels:
    app: vault-installer
  annotations:
    sidecar.istio.io/inject: "false"
spec:
  priorityClassName: platform-critical
  serviceAccountName: vault-installer-sa
  automountServiceAccountToken: true
  containers:
    - name: vault-installer
      image: dockerdev.artifactory.dyn.prd.aws.jpmchase.net/container-external/docker.external.thoughtmachine.io/vault/installer/vault_installer_container:2.4.0
      env:
        - name: CONTAINER_REGISTRY
          value: dockerdev.artifactory.dyn.prd.aws.jpmchase.net/container-external/docker.external.thoughtmachine.io
        - name: NAMESPACE
          value: 105250-core-vault
        - name: MONITORING_NAMESPACE
          value: 105250-vault-monitoring
        - name: CROWN_WHITELIST_FILE
          value: /release-artifacts/packages.txt
        - name: CLOUD_PROVIDER
          value: AWS
        # This variable should be set to the name of the k8s secret that contains the secret needed
        # to pull from the remote repository
        #- name: IMAGE_PULL_SECRETS_NAME
        #  value: 
      command:
        - /bin/bash
        - -c
        - tail -f /dev/null
      volumeMounts:
        - mountPath: /release-artifacts
          name: release-artifacts-volume
        - mountPath: /release-artifacts/hecate-templates
          name: hecate-template-volume
        - mountPath: /release-artifacts/config-templates
          name: config-templates-volume
      resources:
        requests:
          cpu: "0.1"
          memory: 100Mi
        limits:
          cpu: "1.0"
          memory: 2000Mi
  initContainers:
    - name: copy-crown-packages
      image: dockerdev.artifactory.dyn.prd.aws.jpmchase.net/container-external/docker.external.thoughtmachine.io/vault/installer/crown_packages:2.4.0
      command:
        - /bin/sh
        - -c
        - cp -r /release-artifacts/* /release-artifacts-output/
      volumeMounts:
        - mountPath: /release-artifacts-output
          name: release-artifacts-volume
      resources:
        requests:
          cpu: "0.1"
          memory: 100Mi
        limits:
          cpu: "0.3"
          memory: 100Mi
    - name: copy-hecate-artifacts
      image: dockerdev.artifactory.dyn.prd.aws.jpmchase.net/container-external/docker.external.thoughtmachine.io/vault/installer/hecate_container:2.4.0
      command:
        - /bin/sh
        - -c
        - cp -r /release-artifacts/hecate-templates/* /hecate-artifacts-output/
      volumeMounts:
        - mountPath: /hecate-artifacts-output
          name: hecate-template-volume
      resources:
        requests:
          cpu: "0.1"
          memory: 100Mi
        limits:
          cpu: "0.3"
          memory: 100Mi
    - name: copy-config-templates
      image: dockerdev.artifactory.dyn.prd.aws.jpmchase.net/container-external/docker.external.thoughtmachine.io/instances/common/k8s/config_templates:2.4.0
      command:
        - /bin/sh
        - -c
        - cp -r /release-artifacts/config-templates/* /config-templates-out/
      volumeMounts:
        - mountPath: /config-templates-out
          name: config-templates-volume
      resources:
        requests:
          cpu: "0.1"
          memory: 100Mi
        limits:
          cpu: "0.3"
          memory: 100Mi
  volumes:
    - name: release-artifacts-volume
      emptyDir: { }
    - name: hecate-template-volume
      emptyDir: { }
    - name: config-templates-volume
      emptyDir: { }




import json
import logging
import os
import sys
from pathlib import Path

import hcl2

import rotator.config as config


def services_to_skip_for_env(base_tf_path: str, env: str) -> list:
    with open(f"{base_tf_path}/{env}/terraform.tfvars") as fp:
        tf_vars = hcl2.load(fp)
        skipped_modules = tf_vars.get("skipped_modules", [])
        return skipped_modules


def services_to_skip_for_all_envs(base_tf_path: str) -> dict:
    env_map = {}
    env_list = list(os.listdir(base_tf_path))
    for env in env_list:
        services_to_skip = services_to_skip_for_env(base_tf_path, env)
        env_map[env.lower()] = services_to_skip
    return env_map


def create_env_map_file(base_tf_path: str, env_map_dir: str) -> dict:
    env_map = services_to_skip_for_all_envs(base_tf_path)
    Path(env_map_dir).mkdir(exist_ok=True)
    env_map_path = f"{env_map_dir}/{config.env_map_file}"
    logging.info(f"Creating env map file: {env_map_path}")
    with open(env_map_path, "w") as map_file:
        json.dump(env_map, map_file)
        return env_map


def filter_out_services_to_skip(services):
    with open(config.env_map_path) as map_file:
        env_map = json.load(map_file)
        services_to_skip = env_map[config.deploy_env]
        services_to_skip = [service.replace("_", "-") for service in services_to_skip]
        logging.info(
            f"environment: {config.deploy_env}, services_to_skip: {services_to_skip}",
        )
        services = [
            service for service in services if service["name"] not in services_to_skip
        ]
    return services


def main():
    logging.basicConfig(level=logging.INFO)
    if len(sys.argv) != 3:
        print(
            "Builds a JSON map of de-activated services per environment with filename env_map.json\n"
            "Usage: python service_filter.py [tf-eu-west-1-policies-base-path] [output-dir]\n"
            "Example: python src/service_filter.py ./terraform/service-account-policies/eu-west-1/ ./build",
        )
        exit(1)
    create_env_map_file(sys.argv[1], sys.argv[2])


if __name__ == "__main__":
    main()



import os


def get_env(val: str) -> str:
    return os.getenv(val, val)


log_level = os.environ.get("LOGLEVEL", "INFO").upper()
skip_ssl_verify = os.getenv("SKIP_SSL_VERIFY", "false") == "true"

delete_old_accounts = os.getenv("DELETE_OLD_ACCOUNTS", "") == "true"
create_new_accounts = os.getenv("CREATE_NEW_ACCOUNTS", "") == "true"
rotate_accounts_tokens = os.getenv("ROTATE_ACCOUNTS_TOKENS", "") == "true"
rotate_now = os.getenv("ROTATE_NOW", "") == "true"

rotator_service_name = get_env("ROTATOR_SERVICE_NAME")
region_name = get_env("AWS_REGION")
vault_url = get_env("VAULT_URL")
threshold_days = int(os.getenv("TOKEN_ROTATION_THRESHOLD_DAYS", 1))
deploy_env = get_env("DEPLOY_ENV")
deploy_id = get_env("DEPLOY_ID")
seal_id = get_env("SEAL_ID")

env_map_file = "env_map.json"
env_map_path = os.getenv("ENV_MAP_PATH", f"/app/{env_map_file}")

services_map_path = os.getenv("SERVICES_PATH", "/config/services.yaml")



import logging
from collections.abc import Iterable

import yaml

import rotator.config as config
from rotator.secret_manager import SecretManager
from rotator.vault_manager import VaultController
from service_filter import filter_out_services_to_skip

LOGLEVEL = config.log_level
logging.basicConfig(level=LOGLEVEL)

SERVICE_SUFFIXES = ["-alpha", "-bravo"]


class RequestedService:
    def __init__(self, name: str, permissions: tuple = ()):
        self.name = name
        self.permissions = permissions

    def __eq__(self, other):
        return self.name == other.name and self.permissions == other.permissions


def load_services() -> list[RequestedService]:
    with open(config.services_map_path) as f:
        services = yaml.load(f, yaml.SafeLoader)

    services = filter_out_services_to_skip(services)
    logging.info(
        f"Configuring {len(services)} active services: {[s['name'] for s in services]}",
    )

    return [*map(lambda x: RequestedService(*x.values()), services)]


def get_sa_names_for_service(service_name: str) -> set[str]:
    return {service_name + service_suffix for service_suffix in SERVICE_SUFFIXES}


def update_tm_from_req(
    services_req: list[RequestedService],
    sa_names: set[str],
) -> tuple[Iterable[RequestedService], Iterable[RequestedService], set[str]]:
    service_new = list()
    service_exst = list()

    for service_req in services_req:
        sa_req_names = get_sa_names_for_service(service_req.name)

        if sa_req_names.issubset(sa_names):
            service_exst.append(service_req)
        else:
            service_new.append(service_req)

        sa_names = sa_names.difference(sa_req_names)

    return service_new, service_exst, sa_names


class Runner:
    def __init__(self):
        self.secret_manager = SecretManager(
            config.region_name,
            config.seal_id,
            config.deploy_id,
            config.deploy_env,
        )
        self.vault_controller = VaultController()

    def run(self):
        logging.info("Starting... ")
        logging.info(f"threshold_days={config.threshold_days} ")
        logging.info(f"env={config.deploy_env} ")
        logging.info(
            f"delete_old_accounts={config.delete_old_accounts} ",
        )  # from TM (freeze)
        logging.info(f"create_new_accounts={config.create_new_accounts} ")  # in TM
        logging.info(f"rotate_account_tokens={config.rotate_accounts_tokens}")  # in SM
        logging.info(f"rotate_now={config.rotate_now}")  # in TM then SM

        # Initialization
        rotator_token = self.secret_manager.fetch_secret(
            config.rotator_service_name,
        ).fetch_token()

        logging.debug("Rotator token: %s", rotator_token)

        vault = self.vault_controller.init(rotator_token)

        logging.debug(f"Vault accounts: {self.vault_controller}")

        services_req = load_services()

        service_new, service_exst, sa_del = update_tm_from_req(
            services_req,
            vault.get_active_service_account_names(),
        )

        def create_new_service(svc: RequestedService):
            sa_names = sorted(get_sa_names_for_service(svc.name))
            ids, tokens = self.vault_controller.create_sa(sa_names, svc.permissions)
            self.secret_manager.save_secret(svc.name, ids, tokens)

        for service in service_new:
            logging.info("Adding %s%s", "-" * 8, service.name)
            if not config.create_new_accounts:
                logging.info(
                    "Skipping creation %s since create_new_accounts=%s",
                    service.name,
                    config.create_new_accounts,
                )
                continue

            create_new_service(service)

        for service in service_exst:
            logging.info("Updating %s%s", "-" * 8, service.name)

            sa_names_exst = get_sa_names_for_service(service.name)

            self.vault_controller.update_permissions(sa_names_exst, service.permissions)

            sm_secret = self.secret_manager.fetch_secret(service.name)

            # Fixing existing secrets if they are invalid
            if not sm_secret.valid() or config.rotate_now:
                create_new_service(service)
                continue

            if config.rotate_accounts_tokens:
                if self.secret_manager.should_refresh(sm_secret):
                    new_token = self.vault_controller.refresh_token(
                        sm_secret.token_last.sa_id,
                    )
                    self.secret_manager.rotate_secret(sm_secret, new_token)

        for sa_name in sa_del:
            logging.info("Removing %s%s", "-" * 8, sa_name)

            if not config.delete_old_accounts:
                logging.info(
                    "Skipping deletion %s since delete_old_accounts=%s",
                    sa_name,
                    config.delete_old_accounts,
                )
                continue

            self.vault_controller.remove_sa(sa_name)



import datetime
import json
import logging
from collections import defaultdict

import boto3

from rotator.config import threshold_days
from rotator.errors import SecretsManagerError


class SecretToken:
    def __init__(self, sa_id=None, token=None):
        self.sa_id = sa_id
        self.token = token
        self.last_updated = 0 if token is None else datetime.datetime.now().timestamp()


class Secret:
    def __init__(self, arn_name: str, secret_tokens: list[SecretToken]):
        self.arn_name = arn_name
        self.secret_tokens: list[SecretToken] = secret_tokens

    @property
    def token_1(self) -> SecretToken:
        return self.secret_tokens[0]

    @property
    def token_last(self) -> SecretToken:
        return self.secret_tokens[1]

    def valid(self) -> bool:
        return len(self.secret_tokens) == 2

    def fetch_token(self):
        return self.token_1.token

    @staticmethod
    def from_json_response(secret_arn: str, json_response: dict[str, str]):
        raw_secret_tokens: dict[int, SecretToken] = defaultdict(SecretToken)

        if len(json_response) == 1:
            # This is the intial rotator service account - just single value - converting...
            raw_secret_tokens[0] = SecretToken(
                sa_id=-1,
                token=json_response.popitem()[1],
            )
        else:
            for key, val in json_response.items():
                import re

                PATTERN = r"token(\d)_?(.*)"
                m = re.fullmatch(PATTERN, key)

                if m is None:
                    logging.warning(
                        "Cannot understand the pattern in the secret %s: %s",
                        secret_arn,
                        key,
                    )
                    continue

                tkn_id = int(m.group(1))
                tkn_type = m.group(2)

                if not tkn_type:
                    raw_secret_tokens[tkn_id].token = val
                elif tkn_type == "id":
                    raw_secret_tokens[tkn_id].sa_id = val
                elif tkn_type == "last_updated":
                    raw_secret_tokens[tkn_id].last_updated = float(val)

        secret_tokens: list[SecretToken] = [
            raw_secret_tokens[tkn_id]
            for tkn_id in sorted(raw_secret_tokens.keys())
            if None
            not in (raw_secret_tokens[tkn_id].sa_id, raw_secret_tokens[tkn_id].token)
        ]
        return Secret(secret_arn, secret_tokens)

    def serialize(self):
        if not self.valid():
            raise SecretsManagerError(f"Secret {self.arn_name} has wrong values.")

        secret_values = {}

        for idx, secret_token in enumerate(self.secret_tokens):
            secret_values[f"token{idx + 1}"] = secret_token.token
            secret_values[f"token{idx + 1}_id"] = secret_token.sa_id
            secret_values[f"token{idx + 1}_last_updated"] = secret_token.last_updated

        return secret_values


class SecretManager:
    def __init__(self, region_name: str, seal_id: str, deploy_id: str, deploy_env: str):
        self.client = boto3.client(
            service_name="secretsmanager",
            region_name=region_name,
        )
        self.seal_id = seal_id
        self.deploy_id = deploy_id
        self.deploy_env = deploy_env

    def build_secret_name(self, service_name: str) -> str:
        return f"{self.seal_id}-{self.deploy_id}-{self.deploy_env}-{service_name}-tennessee-token"

    def __load_secret(self, secret_arn: str) -> dict[str, str]:
        logging.debug("Loading secret: %s", secret_arn)
        try:
            response = self.client.get_secret_value(SecretId=secret_arn)
            value = response["SecretString"]
        except self.client.exceptions.ResourceNotFoundException:
            logging.warning("AWS secret %s doesn't have value", secret_arn)
            value = "{}"

        logging.debug("Secret successfully extracted from %s", secret_arn)
        return json.loads(value)

    def fetch_secret(self, service_name: str) -> Secret:
        secret_arn = self.build_secret_name(service_name)
        json_response = self.__load_secret(secret_arn)

        return Secret.from_json_response(secret_arn, json_response)

    def save_secret(self, service_name, ids, tokens) -> None:
        arn_name = self.build_secret_name(service_name)
        secret_tokens = [
            SecretToken(sa_id=sa_id, token=token) for (sa_id, token) in zip(ids, tokens)
        ]
        sm_secret = Secret(arn_name, secret_tokens=secret_tokens)
        self.__save_secret(sm_secret)

    @staticmethod
    def should_refresh(sm_secret: Secret) -> bool:
        last_updated = sm_secret.token_1.last_updated
        age = datetime.timedelta(
            seconds=datetime.datetime.now().timestamp() - last_updated,
        )
        logging.debug(
            f"Secret {sm_secret.arn_name} ID {sm_secret.token_1.sa_id} has token age '{age}'",
        )
        return age > datetime.timedelta(days=threshold_days) / 2

    def rotate_secret(self, sm_secret: Secret, token: str):
        prev_last_token = sm_secret.token_last
        sm_secret.secret_tokens = [
            SecretToken(prev_last_token.sa_id, token),
        ] + sm_secret.secret_tokens[:-1]
        self.__save_secret(sm_secret)

    def __save_secret(self, sm_secret: Secret):
        secret_values = sm_secret.serialize()

        self.client.put_secret_value(
            SecretId=sm_secret.arn_name,
            SecretString=json.dumps(secret_values),
        )
        logging.info("Saving secret: %s", sm_secret.arn_name)



apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.service.name }}
  namespace: {{ .Values.namespace }}
  annotations:
{{- if .Values.pcaCertEnabled }}
    dynamo.certificate.tls/active: "false"
    dynamo.certificate.cdb/active: "false"
    pca.icb.tls/active: "true"
{{ else }}
    dynamo.certificate.tls/active: "true"
{{ end }}  
  labels:
    {{- with .Values }}
    {{- include "icb-labels.labels" . | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.service.name }}
  template:
    metadata:
      annotations:
    {{- if .Values.pcaCertEnabled }}
        dynamo.certificate.tls/active: "false"
        dynamo.certificate.cdb/active: "false"
        pca.icb.tls/active: "true"
    {{ else }}
        dynamo.certificate.tls/active: "true"
    {{ end }}        
        sidecar.istio.io/inject: "false"
      labels:
        app: {{ .Values.service.name }}
        {{- with .Values }}
        {{- include "icb-labels.labels" . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName }}
      automountServiceAccountToken: true
      securityContext:
        fsGroup: 999
        runAsUser: 999
        runAsGroup: 999
      containers:
        - name: service
          ports:
            - name: http-actuator
              containerPort: 9090
              protocol: TCP
            - name: http-webhook
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: {{ .Values.livenessProbe.path }}
              port: {{ .Values.livenessProbe.port }}
              scheme: HTTP
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: {{ .Values.readinessProbe.path }}
              port: {{ .Values.readinessProbe.port }}
              scheme: HTTP
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: 3
            timeoutSeconds: 5
          env:
            - name: JAVA_TOOL_OPTIONS
              {{- if .Values.pcaCertEnabled }}
              value: "-Xms{{ .Values.jvm.minHeapSize }} -Xmx{{ .Values.jvm.maxHeapSize }} -Dserver.ssl.key-store=certs/keystore.p12 -Dserver.ssl.key-store-password=${CERT_KEY_STORE_PASSWORD} -Dapp.rootCaPem=file:///certs/ca.crt"
              {{ else }}
              value: "-Xms{{ .Values.jvm.minHeapSize }} -Xmx{{ .Values.jvm.maxHeapSize }}"
              {{ end }}            
          resources:
            requests:
              memory: {{ .Values.resources.requests.memory }}
              cpu: {{ .Values.resources.requests.cpu }}
            limits:
              memory: {{ .Values.resources.limits.memory }}
          image: {{ .Values.image.host }}/dynamo-services/{{ .Values.appName }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}



apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.service.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- with .Values }}
    {{- include "icb-labels.labels" . | nindent 4 }}
    {{- end }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: {{ .Values.service.name }}



apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.component }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- with .Values }}
    {{- include "icb-labels.labels" . | nindent 4 }}
    {{- end }}





apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: "core-vault-webhooks.dynamo.jpmorgan.com"
  labels:
    {{- with .Values }}
    {{- include "icb-labels.labels" . | nindent 4 }}
    {{- end}}
webhooks:
  - name: "a-tls-cert-annotation.dynamo.jpmorgan.com"
    rules:
      - apiGroups: [ "*" ]
        apiVersions: [ "*" ]
        operations: [ "CREATE","UPDATE" ]
        resources: [ "deployments","jobs","statefulsets" ]
    namespaceSelector:
      matchLabels:
        core-vault-webhooks-tls: enabled
    clientConfig:
      service:
        name: core-vault-webhooks
        namespace: 105250-core-vault-webhooks
        port: 8080
        path: /api/v1/tls-cert-annotation
    # Current version of fabric8 is dictated by Spring Cloud: 4.10.3 only supports v1beta1 
    admissionReviewVersions: [ "v1" ]
    timeoutSeconds: 30
    sideEffects: NoneOnDryRun
  - name: "resource-limit.dynamo.jpmorgan.com"
    rules:
      - apiGroups: [ "*" ]
        apiVersions: [ "*" ]
        operations: [ "CREATE","UPDATE" ]
        resources: [ "pods" ]
    namespaceSelector:
      matchLabels:
        core-vault-webhooks-resource-limit: enabled
    clientConfig:
      service:
        name: core-vault-webhooks
        namespace: 105250-core-vault-webhooks
        port: 8080
        path: /api/v1/resource-limit
    # Current version of fabric8 is dictated by Spring Cloud: 4.10.3 only supports v1beta1
    admissionReviewVersions: [ "v1" ]
    timeoutSeconds: 30
    sideEffects: NoneOnDryRun
  - name: "vault-addr.dynamo.jpmorgan.com"
    rules:
      - apiGroups: [ "*" ]
        apiVersions: [ "*" ]
        operations: [ "CREATE","UPDATE" ]
        resources: [ "pods" ]
    namespaceSelector:
      matchLabels:
        core-hault-addr: enabled
    clientConfig:
      service:
        name: core-vault-webhooks
        namespace: 105250-core-vault-webhooks
        port: 8080
        path: /api/v1/vault-addr
    admissionReviewVersions: [ "v1" ]
    timeoutSeconds: 30
    sideEffects: NoneOnDryRun
  - name: "deployment-resiliency.dynamo.jpmorgan.com"
    rules:
      - apiGroups: [ "*" ]
        apiVersions: [ "*" ]
        operations: [ "CREATE","UPDATE" ]
        resources: [ "deployments" ]
    namespaceSelector:
      matchLabels:
        core-vault-webhooks-deployment-resiliency: enabled
    clientConfig:
      service:
        name: core-vault-webhooks
        namespace: 105250-core-vault-webhooks
        port: 8080
        path: /api/v1/deployment-resiliency
    admissionReviewVersions: [ "v1" ]
    timeoutSeconds: 30
    sideEffects: NoneOnDryRun



apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- with .Values }}
    {{- include "icb-labels.labels" . | nindent 4 }}
    {{- end }}
spec:
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      name: https
  selector:
    app: {{ .Values.service.name }}



apiVersion: v2
description: A Helm chart for core-vault-webhooks
name: core-vault-webhooks
version: 1.0.1
dependencies:
  - name: icb-labels
    version: ~0.2.0
    repository: https://artifactory.dyn.prd.aws.jpmchase.net/artifactory/helmdev



pcaCertEnabled: true

appName: icb-ledgers-core-vault-webhooks
component: core-vault-webhooks
namespace: 105250-core-vault-webhooks
serviceAccountName: core-vault-webhooks
port: 8080

service:
  name: core-vault-webhooks
  sealId: "105250"
  squad: ICB_Ledgers
  tribe: ICB_SRE

replicaCount: 3

image:
  pullPolicy: IfNotPresent

livenessProbe:
  path: /actuator/health
  port: 9090
  initialDelaySeconds: 30
  periodSeconds: 20

readinessProbe:
  path: /actuator/health
  port: 9090
  periodSeconds: 20
  initialDelaySeconds: 30

resources:
  requests:
    memory: "512Mi"
    cpu: "150m"
  limits:
    memory: "1Gi"

jvm:
  minHeapSize: "250m"
  maxHeapSize: "700m"




# flake8: noqa
# noqa: E501
import os

# PUSHGW_CONFIG
default_push_gateway_url = (
    "https://pushgateway.dn-dcore.dynamo.dev.eu-west-1.aws.jpmchase.net:443/"
)
PUSHGATEWAY_URL = os.getenv("PUSH_GATEWAY_URL", default_push_gateway_url)
JOB_NAME = os.getenv("PUSH_GATEWAY_JOB_NAME")
# PUSH_GATEWAY_JOB_NAME= os.getenv("PUSH_GATEWAY_JOB_NAME")
CERT_FILE = os.getenv("PUSHGATEWAY_CERT_FILE")
KEY_FILE = os.getenv("PUSHGATEWAY_KEY_FILE")

# AWS_CONFIG
AWS_REGION = os.getenv("AWS_REGION")
S3_BUCKET_HAULT_TOKEN = os.getenv("S3_BUCKET_NAME_HAULT_TOKEN")
S3_OBJECT_HAULT_TOKEN = os.getenv("S3_OBJECT_NAME_HAULT_TOKEN")
AWS_ROOT_CA = os.getenv("AWS_ROOT_CA", "/tls")
# HAULT CONFIG
HAULT_URL = os.getenv("HAULT_URL")
HAULT_SECRETS_PATH = os.getenv("HAULT_SECRETS_PATH")
HAULT_SECRET_CERTS_SUB_PATH = os.getenv("HAULT_SECRET_CERTS_SUB_PATH")

# SECRET_MANAGER_CONFIG
default_aws_tnn_token_ts_key = "token_last_updated"
SECRET_MANAGER_FILTER_TNN = os.getenv("AWS_SECRET_TNN_FILTER")
SECRET_TOKEN1_TO_MONITOR = os.getenv(
    "AWS_SECRET_TNN_TOKEN1_TS_KEY", default_aws_tnn_token_ts_key
)
SECRET_TOKEN2_TO_MONITOR = os.getenv(
    "AWS_SECRET_TNN_TOKEN2_TS_KEY", default_aws_tnn_token_ts_key
)
SECRET_TOKEN_TO_MONITOR_SUFFIX = SECRET_TOKEN1_TO_MONITOR.split("_", 1)[-1]




import json
import logging
import os

import boto3
from psycopg2 import connect, extras

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# DB Identifier
DB_IDENTIFIER = os.getenv("DB_IDENTIFIER", "xcore")

# Specify the AWS region where your secret is stored
AWS_REGION = os.getenv("AWS_REGION", "eu-west-1")
ENV = os.getenv("ENV", "dcore")


def get_rds_secret():
    # Get Credentials from RDS client and Secret Manager
    db_identifier = f"{DB_IDENTIFIER.replace('xcore', ENV)}"
    try:
        # Create a RDS client
        rds_client = boto3.client("rds", region_name=AWS_REGION)
        postgres_db = rds_client.describe_db_clusters(
            DBClusterIdentifier=db_identifier
        )["DBClusters"][0]

        postgres_db_arn = postgres_db["MasterUserSecret"]["SecretArn"]

        secrets_manager = boto3.client("secretsmanager", region_name=AWS_REGION)
        # Retrieve the secret value
        response = secrets_manager.get_secret_value(SecretId=postgres_db_arn)

        # Parse and print the secret JSON
        secret_value = json.loads(response["SecretString"])
        secret_value["host"] = postgres_db["ReaderEndpoint"]
        secret_value["port"] = postgres_db["Port"]

        return secret_value

    except ConnectionError as e:
        logger.error(f"Error: {e}, When trying to retrieve {postgres_db_arn}")
    except Exception as e:
        logger.error(f"Error: {e}")


def execute_query(sql_query, db_name="vault"):
    # Extract URL, username, and password
    secret_value = get_rds_secret()
    db_host = secret_value["host"]
    db_username = secret_value["username"]
    db_password = secret_value["password"]
    db_port = secret_value["port"]
    # Print the extracted values
    logger.info(f"Url: {db_host}")
    logger.info(f"Username: {db_username}")
    logger.info(f"dbname: {db_name}")
    logger.info(f"port: {db_port}")
    result = None
    column_names = ""
    try:
        # Establish a database connection
        connection = connect(
            host=db_host,
            port=db_port,
            dbname=db_name,
            user=db_username,
            password=db_password,
        )
        # Create a cursor object
        with connection.cursor(cursor_factory=extras.RealDictCursor) as cursor:
            # Execute the SQL query
            cursor.execute(sql_query)
            # Fetch all rows from the result set
            data = cursor.fetchall()
            column_names = [
                desc[0] for desc in cursor.description if desc[0].lower() != "value"
            ]
            result = [dict(row) for row in data] if data else None

        # End if there is no more rows
    except json.JSONDecodeError as e:
        logger.error(f"Error parsing JSON: {e}")
    except KeyError as e:
        logger.error(f"Error accessing JSON fields: {e}")
    except Exception as e:
        logger.error(f"Error: {e}")
    finally:
        if connection in locals():
            # Close the cursor and the connection
            cursor.close()
            connection.close()
        logger.info("Connection to database has been closed successfully")
    return column_names, result




{{- if not (eq .Values.clusterName "dn-dinfrc-vault") }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-passthrough: "false"
  labels:
    {{- with .Values }}
    {{- include "icb-labels.labels" . | nindent 4 }}
    {{end}}
  name: {{ .Values.service.name }}
  namespace: {{  .Values.namespace }}
spec:
  rules:
    - host: core-api.{{ .Values.clusterName }}.{{ .Values.CAEnvLevelDNS }}
      http:
        paths:
          - backend:
              service:
                name: core-api-gateway
                port:
                  name: http
            pathType: ImplementationSpecific
    - host: core-api-gateway.{{ .Values.namespace }}.svc.cluster.local
      http:
        paths:
          - backend:
              service:
                name: core-api-gateway
                port:
                  name: http
            pathType: ImplementationSpecific
    - host: workflows-api-gateway.{{ .Values.namespace }}.{{ .Values.CAEnvLevelDNS }}
      http:
        paths:
          - backend:
              service:
                name: workflows-api-gateway
                port:
                  name: http
            pathType: ImplementationSpecific
    - host: workflows-api-gateway.{{ .Values.namespace }}.svc.cluster.local
      http:
        paths:
          - backend:
              service:
                name: workflows-api-gateway
                port:
                  name: http
            pathType: ImplementationSpecific
    - host: workflow-simulator.{{ .Values.namespace }}.{{ .Values.CAEnvLevelDNS }}
      http:
        paths:
          - backend:
              service:
                name: workflow-simulator
                port:
                  name: http
            pathType: ImplementationSpecific
    - host: workflow-simulator.{{ .Values.namespace }}.svc.cluster.local
      http:
        paths:
          - backend:
              service:
                name: workflow-simulator
                port:
                  name: http
            pathType: ImplementationSpecific
    - host: tm-ops.{{ .Values.CAEnvLevelDNS }}
      http:
        paths:
          - backend:
              service:
                name: vault-websocket
                port:
                  name: websocket
            path: /websocket
            pathType: ImplementationSpecific
          - backend:
              service:
                name: vault-admin-website
                port:
                  name: http
            path: /
            pathType: ImplementationSpecific
    - host: tm-documentation.{{ .Values.clusterName }}.{{ .Values.CAEnvLevelDNS }}
      http:
        paths:
          - backend:
              service:
                name: documentation
                port:
                  name: http
            pathType: ImplementationSpecific
    - host: access-control-api-gateway.{{ .Values.namespace }}.svc.cluster.local
      http:
        paths:
          - backend:
              service:
                name: access-control-api-gateway
                port:
                  name: http
            pathType: ImplementationSpecific
{{- end }}



apiVersion: v2
description: A Helm chart for Vault ingress
name: vault-ingress
version: 1.0.0
dependencies:
  - name: icb-labels
    version: ~0.2.0
    repository: https://artifactory.dyn.prd.aws.jpmchase.net/artifactory/helmdev



service:
  name: vault-ingress
  sealId: "105250"
  squad: ICB_Ledgers
  tribe: ICB_SRE

namespace: 105250-core-vault


clusterName: dn-ncore-vault
CAEnvLevelDNS: dynamo.test.eu-west-1.aws.jpmchase.net



FROM dockerprod.artifactory.dyn.prd.aws.jpmchase.net/container-base/managedbaseimages/lrh9/python:3.10-stable

ARG TM_VAULT_VERSION="4.6.33"

RUN set -eux;
ENV PG_VERSION=15
ENV PGHOME=/var/lib/pgsql
ENV PGDATA=${PGHOME}/${PG_VERSION}/data

RUN microdnf module enable postgresql:${PG_VERSION} -y && microdnf install \
    tar  \
    gzip  \
    libicu  \
    perl  \
    libxslt  \
    jq  \
    vim \
    nano \
    shadow-utils  \
    awscli \
    diffutils \
    postgresql \
    openssl \
    bind-utils \
    traceroute \
    nc \
    net-tools \
    -y \
    && microdnf clean all

# Install Pipenv and Install used python packages

COPY Pipfile* .
RUN pip3 install pipenv \
    && pipenv install --system --deploy \
    && pip3 uninstall pipenv --yes \
    && rm -rf Pipfile*

VOLUME /var/run/postgresql

COPY ./release-artifacts/$TM_VAULT_VERSION/vaultctl /release-artifacts/vaultctl
COPY ./release-artifacts/$TM_VAULT_VERSION/release.json /release-artifacts/release.json










avg by (operation) (rate(service_requests{namespace="105250-borrow-temporal", service_name="frontend"}[5m]))


sum by (activity_type) (rate(temporal_activity_execution_latency_seconds_count{namespace=~"105250-borrow", exported_namespace="borrow"}[1m]))

sum by (operation) (rate(service_requests{operation=~"StartWorkflowExecution|SignalWorkflowExecution|SignalWithStartWorkflowExecution", namespace="105250-borrow-temporal"}[1m]))

sum by (workflow_type) (rate(temporal_workflow_failed_total{namespace=~"105250-borrow", exported_namespace="borrow"}[1m]))


sum by (workflow_type, task_queue) (rate(temporal_workflow_task_queue_poll_succeed_total{namespace=~"105250-borrow", exported_namespace="borrow"}[1m]))

histogram_quantile(0.99, sum(rate(syncmatch_latency_bucket{namespace="105250-borrow-temporal"}[1m])) by (operation, le, type))

histogram_quantile(0.95, sum(rate(syncmatch_latency_bucket{namespace="105250-borrow-temporal"}[1m])) by (operation, le, type))

histogram_quantile(0.99, sum(rate(service_latency_bucket{namespace="105250-borrow-temporal", service_name=~"$Service", operation!~"PollActivityTaskQueue|PollWorkflowTaskQueue"}[1m])) by (operation, le))

histogram_quantile(0.95, sum(rate(service_latency_bucket{namespace="105250-borrow-temporal", service_name=~"$Service", operation!~"PollActivityTaskQueue|PollWorkflowTaskQueue"}[1m])) by (operation, le))

histogram_quantile(0.95, sum(rate(persistence_latency_bucket{namespace="105250-borrow-temporal", type="$Service"}[1m])) by (operation, le))

histogram_quantile(0.95, sum(rate(temporal_activity_succeed_endtoend_latency_seconds_bucket{namespace=~"105250-borrow", exported_namespace="borrow"}[1m])) by ( activity_type, le))

histogram_quantile(0.99, sum(rate(temporal_activity_succeed_endtoend_latency_seconds_bucket{namespace=~"105250-borrow", exported_namespace="borrow"}[1m])) by ( activity_type, le))

histogram_quantile(0.99, sum(rate(client_latency_bucket{namespace="105250-borrow-temporal", service_name=~"$Service", operation=~"MatchingClientPollActivityTaskQueue|MatchingClientPollWorkflowTaskQueue"}[1m])) by (operation, le, service_role))

histogram_quantile(0.95, sum(rate(client_latency_bucket{namespace="105250-borrow-temporal", service_name=~"$Service", operation=~"MatchingClientPollActivityTaskQueue|MatchingClientPollWorkflowTaskQueue"}[1m])) by (operation, le, service_role))

(180 - (time() - max(kube_job_status_completion_time{namespace='105250-borrow-temporal', job_name=~'borrow-temporal-origination-long-running-wf-mon-job.+'}))) or on() vector (0)

(time() - max(kube_job_status_completion_time{namespace='105250-borrow-temporal', job_name=~'borrow-temporal-rds-rotation-cron.+'}))


COPY ./release-artifacts/$TM_VAULT_VERSION/vault-$TM_VAULT_VERSION.release /release-artifacts/vault-${TM_VAULT_VERSION}.release

# Install AWS root certs
COPY resources/root.crt ${PGHOME}/.postgresql/root.crt
COPY resources/kubectl /sbin/kubectl
RUN chmod +x /sbin/kubectl
COPY resources/wait.sh /
COPY scripts /scripts
RUN chmod +x wait.sh
RUN chmod -R +x /scripts
# Finish up.

USER 999
EXPOSE 5432

ENTRYPOINT ["/wait.sh"]




apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  labels:
    prom: kubelet
    role: rules
  name: custom-contract-engine-alerts
  namespace: {{.Values.vaultNamespace}}
spec:
  groups:
    - name: contract-engine-resource-usage
      rules:
        - alert: ContractEnginePodHighMemUsage
          # This works under the assumption that CE pods have mem requests 1573741824 bytes
          expr: |
            max by (container,pod,label_app,namespace,kubernetes_namespace)
              (
                project:app:container_memory_working_set_bytes{container="main",label_app=~"contract-engine|contr act-post-balance-update-processor"}
              ) > 1073741824
          for: 15m
          labels:
            severity: error
          annotations:
            summary: "Detected possible memory leak in contract-engine pod {{ `{{ $labels.pod }}` }}"
            description: "Memory usage for {{ `{{ $labels.pod }}` }} in the {{ `{{ $labels.kubernetes_namespace }}` }} namespace has exceeded 1 GB for at least 15 minutes"
            hint: "This is likely a memory leak, and kubernetes is not crash-looping the pod, requiring human-intervention to restart the deployment. Processing of Smart or Supervisor Contract code may be blocked until this action is taken."
            source: "{{ `{{ $labels.pod }}` }}"
            component: "vault"
            group: "{{ `{{ $labels.label_app }}` }}"




{{- if .Values.cronjob.createRepublishCronJob }}
apiVersion: batch/v1
kind: CronJob
metadata:
  labels: &icb-labels
  {{- with .Values}}
    {{- include "icb-labels.labels" . | nindent 4 }}
  {{- end}}
  name: {{ .Values.cronjob.name }}
  namespace: {{ .Values.namespace }}
spec:
  schedule: {{ .Values.cronjob.schedule }}
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      labels:
        app: {{ .Values.cronjob.name }}
        project: vault
    spec:
      ttlSecondsAfterFinished: 600
      backoffLimit: 1
      template:
        metadata:
          labels: *icb-labels
        spec:
          restartPolicy: Never
          securityContext:
            fsGroup: 999
            runAsUser: 999
            runAsGroup: 999
          serviceAccountName: {{ .Values.serviceAccountName }}
          containers:
          - image: "{{ .Values.image.host }}/{{ .Values.image.path }}:{{ .Values.image.tag }}"
            imagePullPolicy: IfNotPresent
            name: {{ .Values.cronjob.name }}
            resources:
                limits:
                    memory: 128Mi
                requests:
                    cpu: 100m
                    memory: 128Mi
            command: [ "/bin/bash" ]
            args:
              - "-ec"
              - |
                ./scripts/vault_republish_post_posting_failures.py --log-level ${LOG_LEVEL} --republish-wait-time-minutes ${REPUBLISH_WAIT_TIME_MINUTES}
            env:
              - name: AWS_DEFAULT_REGION
                value: {{.Values.region}}
              - name: DYN_ENV
                value: "{{.Values.environment}}"
              - name: LOG_LEVEL
                value: "INFO"
              - name: REPUBLISH_WAIT_TIME_MINUTES
                value: "{{.Values.cronjob.republishWaitTimeMinutes}}"
              - name: CORE_API_ENDPOINT_URL
                value: "http://core-api-gateway.{{.Values.vaultNamespace}}:8080"
              - name: REMOVE_POSTING_FOR_FAILED_REPUBLISHING_ENABLED
                value: "{{.Values.cronjob.removePostingForFailedRepublishingEnabled}}"
              - name: PROMETHEUS_PUSHGATEWAY_URI
                value: "push-gateway.icb-ledgers-mon:9091"
              - name: JOB_RUN_ID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.labels['job-name']

{{- end }}




pcaCertEnabled: true

service:
  tribe: ICB_Ledgers
  squad: ICB_Ledgers
  sealId: "105250"
  name: vault-tools

image:
  path: dynamo-services/icb-ledgers-vault-tools

name: vault-tools
namespace: 105250-vault-operators
region: eu-west-1

vaultNamespace: 105250-core-vault

serviceAccountName: vault-tools-sa

dbIdentifierParameter: /tm_vault/db_pointer
dbIdentifierCM: vault-tools-db-connect

dbname: postgres

patch:
  deployment:
    contract_engine_account_scheduling:
      enabled: false
      command_batch_size: 20
    contract_db_data_exporter:
      enabled: false
      command_batch_size: 100
    scheduler_job_poller:
      enabled: false
      command_update_interval_secs: 2
    scheduler_operation_processor:
        enabled: false
        command_db_statement_timeout: 10m
        command_db_total_transaction_timeout: 10m
        command_db_exclude_pooler: ""
    otel_collector:
      enabled: true
    burrow:
      enabled: true
  configMap: ""

# TODO: DYN-608634 to remove during next installation after 4.6.28
ca_injector:
  rds_ca_rsa4096_g1:
    deployed: true
add_Kyverno_labels:
    enabled: true
# TODO: Remove after feature will be implemented THUNDERBAL-205
istio_cni:
  repair_by_delete_pods: true

# TODO: Remove potentially: details DYN-626351
istio_ingress_gateway:
  internal_only: true

autovacuum:
  enable: true
  databases:
    - name: ledger_balances
      schema: ledger_balances
      table: bucket_entries

cronjob:
  createRepublishCronJob: true
  name: vault-tools-republish-cron
  schedule:  "30 3 * * *"
  removePostingForFailedRepublishingEnabled: true
  republishWaitTimeMinutes: 1




pre-commit.yaml

repos:
  - repo: https://github.com/antonbabenko/pre-commit-terraform
    rev: v1.88.4
    hooks:
      - id: terraform_fmt
      - id: terraform_tflint
      - id: terraform_validate
        args:
          - --tf-init-args=-no-color
          - --tf-init-args=-backend=false
          - --tf-init-args=-input=false
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: check-yaml
        exclude: helm-chart
      - id: check-json
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-added-large-files
