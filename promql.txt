group by (uri) (http_client_requests_seconds_bucket{service="borrow-account-closure-service"})

group by (client_name) (http_client_requests_seconds_bucket{service="borrow-balance-service"})

group by (uri) (http_client_requests_seconds_bucket{app="borrow-repayment-manager"})

group by (client_name) (http_client_requests_seconds_bucket{service="borrow-account-closure-service"})

count by (__name__) ({service="borrow-eligibility-service", le="+Inf"})

group(istio_request_bytes_bucket{service="borrow-payment-transfer-service", le="+Inf"}) by (destination_service_name)

group by (uri) ({service="borrow-line-manager"})

count by (__name__) ({service="borrow-line-manager", pod="borrow-line-manager-678dc6c89f-j2qf8", le="+Inf"})

group(kafka_consumergroup_lag{consumergroup="borrow-line-manager"}) by (topic)

{app="borrow-loan-manager || borrow-loan-manager-api"}

group(istio_request_bytes_bucket{service="borrow-payment-transfer-service", le="+Inf"}) by (destination_service_name)

